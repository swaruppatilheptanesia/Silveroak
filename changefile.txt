# Change log — files modified by Claude, grouped by enhancement.
# Each section: a comment describing the enhancement, then the unique files changed for it (no repeats).

# Enhancement: Student Profile → Skills tab — collapse long skill-suggestion lists
# (Technical Skills / Domain Interests / Preferred Locations) with a "Show more / Show less"
# toggle. Opt-in via a new `collapsible` prop so Project technologies & Portfolio dialog are unaffected.
src/components/shared/MasterSuggestionChips.tsx
src/pages/Profile.tsx

# Enhancement: Student Profile → Projects — make GitHub URL and Demo URL optional.
# FE: drop the required-presence checks (still validate format if a value is typed) and the
# RequiredLabel asterisks ("(optional)" labels). BE: relax the Zod project schema so both URLs
# are optional+nullable (DB columns were already nullable; no migration).
src/pages/Profile.tsx
docs/silveroak_backend/src/modules/students/student.schema.ts

# Enhancement: Student Profile → Employment — restrict Employment Type to Full-time / Part-time only.
# Removed the "Business" dropdown option and coerced any legacy `employment_type: 'business'`
# record to Full-time on load (normalizeEmploymentTypeValue). FE-only; backend already accepts a
# free string (no enum, no migration); existing rows stay valid.
src/pages/Profile.tsx

# Enhancement: My Portfolio — remove the Settings tab; move the Portfolio Summary stats card to
# above the tab bar (always visible); rename the "Showcases" tab to "Internships" (value kept as
# 'showcases'); remove the "Visible to recruiters" publish toggle (header + settings). FE-only;
# portfolio status defaults to 'published' so recruiter/faculty visibility is unaffected, no migration.
src/pages/Portfolio.tsx

# Enhancement: Student Management — selectable bulk verify + posting-type filter & column.
# Verification tab: per-row checkboxes + "Verify Selected" (reuses existing bulk endpoint; kept
# Verify-All) + posting-type filter + posting-type badges per student. All Students tab: posting-type
# filter + "Posting Types" column. Backend: added posting_type_master_id filter to the student-list
# query (schema + buildStudentWhere via applications relation). No DB migration. Posting type shown
# from each student's applications (empty "—" when none).
docs/silveroak_backend/src/modules/admin/admin.schema.ts
docs/silveroak_backend/src/modules/admin/admin.service.ts
src/types/admin.ts
src/components/admin/VerificationTab.tsx
src/components/admin/StudentListTab.tsx

# Bugfix: Student Management → Selection Database posting-type filter did nothing. The request sent
# the param as `posting_type_master_id`, but this endpoint's backend schema only reads `posting_type`
# (enum job|internship|stipend_internship, matched against the row's flattened value). Renamed the key
# to `posting_type`. Value binding (option.value) was already correct for this value-based endpoint
# (distinct from the UUID-based Applications/Student-list endpoints). FE-only, one line, no migration.
src/components/admin/SelectionDatabaseTab.tsx

# Enhancement: Postings create/edit form — removed "Application Override"; "Job Description PDF"
# now supports MULTIPLE PDFs; "Student Visibility" section commented out (hidden, posting type kept
# per-role); "Location" is now MULTIPLE cities via free-text chips; Salary/Stipend use preset range
# buckets (e.g. "3 - 6 LPA") stored as text. Legacy-mirror strategy: new array columns added, but the
# legacy scalar columns (location, job_description_pdf_url) keep mirroring the first element so most
# display/filter/offer/export sites need no change. Detail pages render the full arrays.
#
# ⚠ MIGRATION (APPLIED 2026-06-04 by user via `npx prisma migrate dev`) — 2 additive columns on Posting:
#   - locations               String[] @default([])
#   - job_description_pdf_urls String[] @default([])
#   Migration: docs/silveroak_backend/prisma/migrations/20260604081429_posting_multiple_location_and_file/migration.sql
docs/silveroak_backend/prisma/schema.prisma
docs/silveroak_backend/prisma/migrations/20260604081429_posting_multiple_location_and_file/migration.sql
docs/silveroak_backend/src/modules/postings/posting.schema.ts
docs/silveroak_backend/src/modules/postings/posting.service.ts
src/types/posting.ts
src/lib/postingModule.ts
src/components/postings/LocationChipsInput.tsx
src/components/postings/MultiPostingForm.tsx
src/components/postings/PostingForm.tsx
src/pages/OpportunityDetail.tsx
src/pages/admin/PostingDetail.tsx

# Enhancement: Create Posting — removed the "Create & Add Another" action. Dropped the three
# secondary* props from the MultiPostingForm usage and the now-unused handleCreateAndAddAnother
# handler. FE-only, one file; MultiPostingForm hides the secondary button when the props are absent.
# Remaining actions (Create Drafts, Create and Publish) untouched. No backend/migration.
src/pages/admin/CreatePosting.tsx

# Enhancement: Employment — multiple entries, removed Duration, close-with-mandatory-completion-proof.
# The Employment tab changed from a single record to a LIST: students add multiple entries (type, company,
# designation, package — Duration REMOVED). Adding an entry now REQUIRES a mandatory offer-letter document
# (POST /me/employments is multipart; controller 400 FILE_REQUIRED if missing; stored in the existing
# offer_letter_url column — no extra migration). Each entry can be Closed, which REQUIRES uploading a completion
# proof document; once closed it is locked (read-only). Both active & closed entries are deletable (confirm).
# Entry cards show the Offer letter (and, when closed, the Completion proof) as links.
# Model relaxed from one-to-one (student_id @unique) to one-to-many. The Employment tab + admin student detail
# were updated to render the list. is_currently_working is kept (= status==='active') for admin display.
#
# Adversity handled: (1) API shape employment(object)→employments(array) — updated every reader (getMyProfile,
# admin getStudentDetail + include, FE ApiStudentProfile + types/admin, admin dialogs); (2) recalcProfileCompletion
# does NOT read employment (verified) so completion scoring unaffected; (3) mandatory completion proof enforced
# both FE (close dialog disables confirm until a file is chosen) and BE (400 FILE_REQUIRED); (4) closed entries
# locked + a second close rejected (422 EMPLOYMENT_ALREADY_CLOSED); (5) existing single rows survive as the first
# entry; legacy offer_letter_url/business_proof_url columns left untouched.
#
# ⚠ MIGRATION REQUIRED — run `npx prisma migrate dev`. schema.prisma (CurrentEmployment):
#   - DROP the `student_id` @unique (now one-to-many) + add @@index([student_id])
#   - DROP column `duration`
#   - ADD columns: status (VarChar default 'active'), closed_at (DateTime?), completion_proof_url (Text),
#     completion_proof_name (VarChar), created_at (default now())
#   - Student relation `current_employment CurrentEmployment?` → `employments CurrentEmployment[]`
#   Then restart the backend (tsx watch) — the routes changed to /me/employments(/:id/close|delete).
docs/silveroak_backend/prisma/schema.prisma
docs/silveroak_backend/src/modules/students/student.service.ts
docs/silveroak_backend/src/modules/students/student.schema.ts
docs/silveroak_backend/src/modules/students/student.controller.ts
docs/silveroak_backend/src/modules/students/student.routes.ts
docs/silveroak_backend/src/modules/admin/admin.service.ts
src/types/student.ts
src/types/admin.ts
src/services/studentService.ts
src/hooks/use-student-api.ts
src/components/employment/EmploymentCloseDialog.tsx
src/components/employment/StudentEmploymentTab.tsx
src/pages/Profile.tsx
src/components/admin/AdminStudentDetailsDialog.tsx
src/components/admin/StudentListTab.tsx

# Enhancement: Student placement opt-out — global + per-posting-type, with reason, confirm-lock, history.
# Students can declare "not interested in placement" entirely (global switch) or per posting type, from a
# new Profile → "Placement" tab. Opting out requires a REASON + confirm; re-enabling is self-service
# (simple confirm). Every enable/disable is recorded in a history table (shown collapsibly in the tab).
# "Show but do not allow register": postings of an opted-out type stay VISIBLE but apply/register-interest
# is blocked — FE disables the Apply button with a reason; BE guards apply() + registerInterests().
#
# Adversity handled: (1) global overrides per-type (enforcement checks global first; per-type switches
# greyed while global is ON); (2) reason required only on opt-out (Zod superRefine + FE dialog gate);
# (3) no posting visibility change — only the apply/interest guards + Apply button; (4) coexists with the
# positive InterestRegistration concept (separate table) — registerInterests also blocks opted-out types;
# (5) existing applications kept (opt-out blocks only NEW applies); (6) history stores a posting_type_label
# snapshot + plain id (no FK) so it survives a posting-type rename/delete; per-type pref FK onDelete:Cascade.
#
# ⚠ MIGRATION REQUIRED — run `npx prisma migrate dev` (additive). schema.prisma changes:
#   - Student: + placement_opt_out (Boolean default false), placement_opt_out_reason (Text), placement_opt_out_at (DateTime?)
#   - new table student_posting_type_preferences (student_id, posting_type_master_id, interested, reason; unique[student_id,type])
#   - new table student_placement_preference_history (scope, posting_type_master_id?, posting_type_label?, interested, reason)
#   - MasterOption back-relation (PostingTypePreference)
#   Then restart the backend (tsx watch) if the new /placement-preferences routes 404.
docs/silveroak_backend/prisma/schema.prisma
docs/silveroak_backend/src/shared/utils/placement-interest.ts
docs/silveroak_backend/src/modules/applications/application.service.ts
docs/silveroak_backend/src/modules/students/student.service.ts
docs/silveroak_backend/src/modules/students/student.schema.ts
docs/silveroak_backend/src/modules/students/student.controller.ts
docs/silveroak_backend/src/modules/students/student.routes.ts
src/types/student.ts
src/services/studentService.ts
src/hooks/use-student-api.ts
src/components/placement/PlacementOptOutDialog.tsx
src/components/placement/StudentPlacementTab.tsx
src/pages/Profile.tsx
src/pages/OpportunityDetail.tsx

# Enhancement: Top bar / branding — logo-only sidebar header + user-type chip menu.
# (1) Sidebar header now shows the logo image only (removed the "SOU" short-name title AND the
# portal-name line), enlarged + centered. (2) Replaced the standalone top-right Logout button with a
# user-type chip (avatar initials + role badge + name + caret) that opens a dropdown menu containing
# "Change Password" and "Logout". Change Password opens a new reusable ChangePasswordDialog (reuses
# authService.changePassword, same validation as the profile Security tab). Logout flow unchanged
# (DashboardLayout.handleLogout passed into the chip). New getRoleLabel/ROLE_LABELS helper covers all
# 7 roles. FE-only, no DB / no migration. Shared layout = applies to every role.
src/lib/permissionModule.ts
src/components/shared/ChangePasswordDialog.tsx
src/components/layout/UserMenu.tsx
src/components/layout/DashboardLayout.tsx
src/components/layout/AppSidebar.tsx

# Enhancement: Policy ↔ Posting Type linkage + mandatory GLOBAL-policy acceptance gate.
# Policy creation/edit now has an OPTIONAL posting-type selector. A policy with NO posting type =
# "global": global policies (a) pop up at student registration as a hard gate ("only when accepted is
# he registered" — enforced via ProtectedRoute redirect to a new /student/policy-gate until accepted)
# and (b) are listed with accepted/pending status in a new "Policies" tab in the student My Profile.
# Posting-type-LINKED policies are captured for future use and are NOT surfaced to students yet.
# Acceptance simplified to a single "I have read and accept this policy" checkbox.
#
# Adversity handled: (1) placement-apply gate (student.policy_accepted via areAllVisiblePoliciesAccepted)
# scoped to GLOBAL policies only (buildVisiblePolicyWhere + policy buildPolicyWhere) so linked policies
# can't deadlock applying; (2) acceptance schema relaxed (4 sharing consents optional) — safe, nothing
# reads those columns for enforcement; legacy /policy page (posts all-6-true) still valid; (3) FK
# onDelete:SetNull (deleting a posting type just makes its policies global). pending_policy_count added
# to the student profile response to drive the gate without redirect loops on tenants with no global policy.
#
# ⚠ MIGRATION REQUIRED — run `npx prisma migrate dev` (additive, nullable). schema.prisma changes:
#   - Policy.posting_type_master_id  String?  + FK posting_type_master -> MasterOption (onDelete: SetNull)
#   - MasterOption.policies_using_as_type  Policy[]  (back-relation "PolicyPostingType")
#   - new @@index([posting_type_master_id]) on policies
#   Then restart the backend (tsx watch) if the new ?global filter / route 404s.
docs/silveroak_backend/prisma/schema.prisma
docs/silveroak_backend/src/modules/policies/policy.schema.ts
docs/silveroak_backend/src/modules/policies/policy.service.ts
docs/silveroak_backend/src/modules/students/student.service.ts
docs/silveroak_backend/src/modules/students/student.schema.ts
src/types/policy.ts
src/types/student.ts
src/components/admin/AddPolicyDialog.tsx
src/components/admin/EditPolicyDialog.tsx
src/pages/admin/PolicyRepository.tsx
src/components/policies/GlobalPolicyCard.tsx
src/components/policies/StudentPoliciesTab.tsx
src/pages/StudentPolicyGate.tsx
src/pages/Profile.tsx
src/pages/Login.tsx
src/App.tsx
src/components/shared/ProtectedRoute.tsx
src/hooks/use-student-api.ts

# Enhancement: Student → My Applications — show posting type as a friendly, app-consistent badge.
# Posting type was already shown but as the raw enum (job/internship/stipend_internship) in a plain
# secondary Badge. Swapped to the shared PostingTypeBadge (formatPostingTypeLabel → "Placement (Job)"
# / "Internship" / "Stipend Internship") in BOTH the Applications tab and the Offers tab. The Offers
# tab now reads offer.posting.type (full vocabulary incl. stipend_internship) instead of the coarser
# offer.type ('job'|'internship'). FE-only, one file, no backend/schema/migration (data already in
# the API responses).
src/pages/MyApplications.tsx

# Enhancement: Resume Manager — allow only PDF uploads. BE: pointed resumeUpload at the existing
# PDF_MIME_TYPES (was the shared RESUME_MIME_TYPES) + "Only PDF files are allowed" message — left
# RESUME_MIME_TYPES/policyDocumentUpload/nocOfferLetterUpload/SUPPORTING_DOCUMENT_MIME_TYPES
# untouched (adversity avoided). FE: accept="application/pdf,.pdf", hard client-side PDF guard before
# upload, and PDF-only helper text. Existing non-PDF resumes stay valid. No migration.
docs/silveroak_backend/src/middleware/upload.ts
src/pages/Resumes.tsx

# Enhancement: NOC requests — cleaner company capture + verifiable Company link + optional end date.
# (1) Create form: Company Name is now a dropdown of existing tenant companies with an "Other → type a
#     new one" escape; State is a built-in Indian-states dropdown (+ "Other"); City and Designation are
#     free-text with datalist autocomplete of previously-used values; added optional Company PAN + GST
#     (soft format validation) and one optional Supporting Document upload (PAN card / GST cert / reg
#     proof). (2) NOC now find-or-creates a real Company row (source='student', verification_status=
#     'pending'); when the NOC is issued the linked Company flips to 'verified' (best-effort, can't break
#     issuance). Matched companies are linked, never overwritten (fill-only-if-empty pan/gst/city/state).
#     (3) End Date is no longer mandatory anywhere — FE posts null when blank; BE Zod + Prisma made
#     optional; certificate renderer + all list/detail/admin date renders guard null ("Ongoing").
#     Posting type "where applied" was already shown in both list views (no change). Admin/student NOC
#     detail now surfaces PAN/GST, the supporting-doc link, and an "Added by Student" + company-verified
#     badge. New student-safe read endpoint GET /noc/field-suggestions powers the Name/City/Designation
#     options; new POST /noc/supporting-document upload (mirrors offer-letter). FE compiles clean; BE
#     compiles clean.
# ⚠ MIGRATION REQUIRED — schema.prisma:
#   • New enum CompanySource { admin student recruiter import }.
#   • Company: + source (default admin), + verification_status (default verified), + pan, gst, city,
#     state; + noc_requests back-relation. (Defaults chosen so existing companies need NO backfill.)
#   • NocRequest: + company_id (FK Company, onDelete SetNull) + index, + company_pan, company_gst,
#     supporting_document_url, supporting_document_name; end_date changed to NULLABLE (DateTime?).
#   Run: cd docs/silveroak_backend && npx prisma migrate dev  (then restart the backend if new
#   /noc/field-suggestions or /noc/supporting-document routes 404).
docs/silveroak_backend/prisma/schema.prisma
docs/silveroak_backend/src/modules/noc/noc.schema.ts
docs/silveroak_backend/src/modules/noc/noc.service.ts
docs/silveroak_backend/src/modules/noc/noc.controller.ts
docs/silveroak_backend/src/modules/noc/noc.routes.ts
docs/silveroak_backend/src/modules/noc/noc-certificate.renderer.ts
docs/silveroak_backend/src/middleware/upload.ts
docs/silveroak_backend/src/modules/admin/admin.service.ts
src/types/noc.ts
src/types/admin.ts
src/lib/indianStates.ts
src/lib/nocModule.ts
src/services/nocService.ts
src/hooks/use-noc-api.ts
src/components/noc/NOCRequestWizard.tsx
src/components/noc/NOCRequestCard.tsx
src/components/noc/NOCRequestDetailSheet.tsx
src/components/noc/NOCReviewDialog.tsx
src/pages/admin/AdminNOCManagement.tsx

# Enhancement: TPO Admin → NOC Management — surface the posting type as its own column.
# The posting type (request.program) was already shown but only as small muted text tucked under the
# NOC-type badge in the "Request" column (easy to miss). Split it into a dedicated "Posting Type" column
# rendered as an outline Badge (consistent with the student NOC cards). NOC type stays its own column.
# FE-only, one file, no backend/schema/migration.
src/pages/admin/AdminNOCManagement.tsx

# Bugfix: Application Pipeline ("Application Records") — rename "Student" to "Profile".
# In the Application Records table both the column header AND the per-row ghost button were labelled
# "Student"; the button opens the student profile dialog (AdminStudentDetailsDialog). Relabeled both the
# header and the button to "Profile" to match what it shows/opens.
# FE-only, one file, no backend/schema/migration.
src/pages/admin/ApplicationPipeline.tsx
src/pages/admin/ApplicationsManagement.tsx

# Enhancement: TPO Admin NOC Management — show current stage + view uploaded documents from the table.
# (1) Added a "Stage" column phrased as where the NOC currently sits / with whom ("Faculty Coordinator",
#     "TPO Cell", "TPO Cell — ready to issue", "Completed", "Closed") via new getNocStageLabel() helper —
#     so faculty-stage NOCs (already returned to TPO under "All Requests") are clearly visible, not just
#     surfaced after faculty approval. (2) Added a "Documents" column with per-row "Offer letter" /
#     "Company proof" view buttons that open the uploaded files in a new tab (resolveBackendAssetUrl);
#     rows without docs show "—". No workflow/permission change (TPO still can't approve a pending_faculty
#     NOC). FE-only.
# No migration (FE-only). Note: the "Company proof" link only has data once the earlier NOC migration is
# run (supporting_document_url column); offer letter works regardless.
src/lib/nocModule.ts
src/pages/admin/AdminNOCManagement.tsx

# Bugfix: TPO NOC Management table was too wide → the whole page scrolled horizontally, and the
# Posting Type / Status badges were wrapping to multiple lines.
# (1) Kept "Posting Type" and "Status" as separate columns but added `whitespace-nowrap` to those (and
#     the Stage) headers/badges so the text no longer wraps and each column takes the width it needs.
# (2) Root cause of the PAGE scrolling (instead of just the table): SidebarInset is a flex child with
#     no min-width:0, so a wide table forced the whole content area wider than the viewport. Added
#     `min-w-0` to SidebarInset so the table's own overflow-x-auto wrapper scrolls horizontally and the
#     page no longer scrolls left. (min-w-0 only lets the flex child shrink — pages that already fit are
#     unaffected.) FE-only, no migration.
src/pages/admin/AdminNOCManagement.tsx
src/components/layout/DashboardLayout.tsx

# Enhancement: Block apply / register-interest when the student has a self-placed NOC for the same posting type.
# A non-rejected self-sourced NOC (pending_faculty/pending_tpo/pending_company_verification/approved/issued)
# now blocks the student from applying to — and registering interest in — postings of that SAME posting type
# (other types stay open; university-drive NOCs never block — those are governed by the offer block).
# BE: new shared guard self-placed-noc-block.ts (assertNoSelfPlacedNoc + getSelfPlacedNocProgramValues),
# called in application.service.apply() (after assertPlacementInterest) and student.service.registerInterests()
# (after the per-type opt-out check). Throws BusinessRuleError 'SELF_PLACED_NOC_BLOCK' (HTTP 422), mirroring
# offer-block.ts / placement-interest.ts. Matches NOC.program (= posting-type MasterOption.value) to the
# posting's type by trimmed/lowercased value. FE: OpportunityDetail surfaces it as a readiness reason
# (disables Apply + explains); the apply/interest 422 message also flows through to the toast as a fallback.
# No migration (query-only).
docs/silveroak_backend/src/shared/utils/self-placed-noc-block.ts
docs/silveroak_backend/src/modules/applications/application.service.ts
docs/silveroak_backend/src/modules/students/student.service.ts
src/pages/OpportunityDetail.tsx

# Enhancement: Create Announcement — drop Batch & Department targeting from the UI, add Semester targeting,
# add a single PDF/image attachment.
# Audience dropdown is now: All Students / Specific Semester / Eligible for Posting (Batch + Department
# options + their checkbox blocks removed). New "Specific Semester" shows a 1–8 checkbox group. New
# optional Attachment field accepts one PDF or image (JPG/PNG/WEBP), uploaded first (POST
# /announcements/attachments, upload-first like policies) then referenced in the create/update payload;
# the student announcement detail shows an "Open attachment" link.
# Read-side targeting: studentMatchesAnnouncement gained a `semester` case matching the student's
# AcademicProfile.semester against target_semesters (reuses matchesAllowedValue). Batch/department columns,
# enum values, and read-side cases are KEPT for back-compat so legacy announcements still reach their
# original audience; only the create UI dropped them. Editing a legacy batch/department announcement coerces
# its audience to "All" (no UI option). Circular auto-fill no longer force-switches audience to department.
#
# ⚠ MIGRATION REQUIRED — run `npx prisma migrate dev`, then restart the backend (new
# /announcements/attachments route). schema.prisma (additive only):
#   - enum TargetAudienceType: + `semester`
#   - Announcement: + target_semesters String[] @default([]); + attachment_url (Text),
#     attachment_name (VarChar 255), attachment_mime_type (VarChar 120), attachment_size (Int?)
#   (target_batches / target_departments columns + enum values intentionally retained.)
docs/silveroak_backend/prisma/schema.prisma
docs/silveroak_backend/src/modules/announcements/announcement.schema.ts
docs/silveroak_backend/src/modules/announcements/announcement.service.ts
docs/silveroak_backend/src/modules/announcements/announcement.controller.ts
docs/silveroak_backend/src/modules/announcements/announcement.routes.ts
docs/silveroak_backend/src/middleware/upload.ts
src/types/announcement.ts
src/services/announcementService.ts
src/hooks/use-announcement-api.ts
src/lib/announcementModule.ts
src/pages/admin/AnnouncementManagement.tsx
src/pages/StudentAnnouncements.tsx

# Bugfix: No Dues Approval — hide the "Issue NDC" action from the admin UI.
# In the No-Dues request detail sheet, an approved request showed an admin-only "Issue NDC" button (and a
# non-admin "Admin-only action" notice) that minted the final NDC number. Removed that button/notice block
# so issuing isn't a required/visible step in the portal. Also dropped the stale "Use Issue NDC separately"
# line from the Change-Status dialog. Backend issue endpoint + service left intact (UI-only, per request);
# Edit Form / Change Status actions remain. No backend/schema/migration.
src/pages/admin/NoDuesManagement.tsx

# Enhancement: No Dues Management — "Export All" to Excel (.xlsx) with full details + document URLs.
# Added an Export All button to the No Dues list toolbar. It fetches every request matching the
# currently selected status filter (loops all pages), and downloads an .xlsx with one column per
# detail field (student info, exit-path-specific fields, status/review metadata) plus clickable
# document URL columns (Offer Letter, Admission Letter, Proof, Certificate — resolved to absolute
# URLs). Reuses downloadExcelTable (src/lib/spreadsheetExport.ts) + noDuesService.getRequests +
# normalizeNoDuesRequest. Frontend-only — no backend/schema/migration.
src/pages/admin/NoDuesManagement.tsx

# Enhancement: Student sidebar — "new"/unread dot on Announcements, Circulars, and My Events & Drives.
# The student left-sidebar nav items now show a small primary-colored dot when the surface has new content.
# Announcements uses the real backend unread flag (my_receipt.is_read). Circulars and Drives have no read
# flag, so they use a client-side "new since last visit" heuristic: a per-surface last-seen timestamp in
# localStorage (key sou:lastSeen:<surface>); an item is new when created_at > lastSeen. Opening the
# Circulars/Drives page marks the surface seen (clears the dot). New shared store + combined hook in
# use-student-new-indicators.ts (useSyncExternalStore + module listener set for same-tab reactivity).
# The three list hooks gained an optional `enabled` param (default true) so the sidebar gates the queries to
# students only. Frontend-only — no backend/schema/migration.
src/hooks/use-student-new-indicators.ts
src/hooks/use-announcement-api.ts
src/hooks/use-circular-api.ts
src/hooks/use-event-api.ts
src/components/layout/AppSidebar.tsx
src/pages/StudentCirculars.tsx
src/pages/StudentDrives.tsx

# Enhancement: Replace the Lovable favicon with the SOU icon (browser tab + social preview).
# index.html had no favicon <link> (browser fell back to the Lovable public/favicon.ico) and the
# og:image/twitter:image pointed at lovable.dev. Generated a square SOU favicon from src/assets/sou-logo.png
# (padded to square, sized to 64px) + a 180px apple-touch-icon, added explicit <link rel="icon"> /
# apple-touch-icon tags, repointed og/twitter images to /sou-logo.png, and deleted public/favicon.ico.
# Static assets only — no code/build/migration.
index.html
public/favicon.png
public/apple-touch-icon.png
public/sou-logo.png

# Enhancement: Hide the offer-letter / certificate document from Recruiters (internship detail).
# The shared InternshipDetailSheet (Recruiter -> Internships -> open an internship) rendered the internship's
# certificate_url — uploaded as "Offer letter / internship certificate" — as a "Completion Certificate ->
# Open certificate" link, the only offer-letter-type document a recruiter could open. Gated that anchor on
# !isRecruiter (useRole from RoleContext) so recruiters no longer see/open the document; the Uploaded/Pending
# status badge stays. Admin/student views unchanged. UI-only per request — certificate_url is still in the API
# payload (known boundary). No backend/schema/migration.
src/components/internships/InternshipDetailSheet.tsx

# Enhancement: Event Type is now a tenant-configurable Master (manageable + drives event creation).
# ⚠ MIGRATION REQUIRED: schema.prisma — add `event_type` to enum MasterCategory; change Event.type from
#   enum `EventType` to `String @db.VarChar(150)`; DROP the `EventType` enum. Prisma's auto-migration can't
#   cast enum→varchar when the table has data, so two HAND-AUTHORED migrations were added (explicit USING cast
#   preserves existing values — no data loss):
#     prisma/migrations/20260605170000_event_type_master/migration.sql        (ADD VALUE 'event_type')
#     prisma/migrations/20260605170100_event_type_string_column/migration.sql (events.type -> VARCHAR(150) + DROP TYPE)
#   Apply with `npx prisma migrate deploy` (does NOT reset), then restart BE. `migrate dev` also works.
# Backend: masters.service.ts seeds the 5 existing event types as event_type masters (ensureTenantEventTypeSeeded,
#   mirrors noc_type seeding). event.schema.ts loosens create/query `type` from z.enum to z.string().min(1).max(150).
#   event.service.ts drops the EventType cast/import and adds assertValidEventType() — event create/update now
#   reject a `type` that isn't an active event_type master (replaces the DB-enum guarantee).
# Frontend: event_type registered in the Masters UI (master.ts union + MasterDataManagement config/orders, no
#   targeting). New useEventTypeOptions hook (mirrors usePostingTypeOptions). New getEventTypeLabel(value) in
#   types/event.ts (legacy pretty labels, else humanized) + ApiEventType loosened to string. EventEditorDialog
#   dropdown now master-driven. All type→label display sites (drives pages, detail/circular dialogs, reports) and
#   the admin Drives type filter now use getEventTypeLabel / the master options so admin-added types render.
docs/silveroak_backend/prisma/schema.prisma
docs/silveroak_backend/src/modules/masters/masters.service.ts
docs/silveroak_backend/src/modules/events/event.schema.ts
docs/silveroak_backend/src/modules/events/event.service.ts
src/types/master.ts
src/types/event.ts
src/pages/admin/MasterDataManagement.tsx
src/hooks/use-event-type-options.ts
src/components/drives/EventEditorDialog.tsx
src/components/drives/EventDetailDialog.tsx
src/components/circulars/GenerateCircularDialog.tsx
src/components/reports/EventAttendanceReport.tsx
src/components/reports/DriveCompletionSummary.tsx
src/components/reports/StudentParticipationHistory.tsx
src/pages/admin/DrivesManagement.tsx
src/pages/StudentDrives.tsx
src/pages/faculty/FacultyDrives.tsx
src/pages/recruiter/RecruiterDrives.tsx

# Bugfix: Master Data page crashed on open ("undefined is not an object (groupedMasters[section].length)").
# The new event_type category was added to ADMIN_SECTION_ORDER but not to the Record<MasterCategory,_>
# state initializers, so groupedMasters['event_type'] / draftValues['event_type'] were undefined. Added
# `event_type` to draftValues and the emptyGroups initializer (MASTER_CATEGORY_CONFIG already had it).
src/pages/admin/MasterDataManagement.tsx

# Enhancement: Posting form — free-text salary/stipend range + keep original Job Description PDF names.
# (1) CTC (Package Range) and Stipend Range changed from preset-bucket <Select> to free-text <Input> in both
#     PostingForm and MultiPostingForm (per-role). Admins now TYPE a range (e.g. "3 - 6 LPA"). Backend ctc/stipend
#     are already free strings (z.string().max(100)); all display sites render the raw value. No migration.
#     CTC_RANGE_OPTIONS/STIPEND_RANGE_OPTIONS constants kept in postingModule (now unused by the forms).
# (2) Job Description PDFs keep their ORIGINAL filename instead of "PDF 1 / PDF 2". Added a parallel,
#     index-aligned job_description_pdf_names String[] (mirrors the urls array). The name is taken from the
#     browser File.name at upload time (upload endpoint unchanged). Upload appends to both arrays, remove splices
#     both by index, payload builder zips+filters as pairs so urls<->names never drift. Forms + student
#     OpportunityDetail + admin PostingDetail render the real name, falling back to "PDF N" for legacy/unnamed.
# ⚠ MIGRATION REQUIRED: schema.prisma — add `job_description_pdf_names String[] @default([])` to Posting
#     (purely additive, auto-migratable; existing rows get [] and show the "PDF N" fallback). Run
#     `npx prisma migrate dev` then restart BE.
docs/silveroak_backend/prisma/schema.prisma
docs/silveroak_backend/src/modules/postings/posting.schema.ts
docs/silveroak_backend/src/modules/postings/posting.service.ts
src/types/posting.ts
src/lib/postingModule.ts
src/components/postings/PostingForm.tsx
src/components/postings/MultiPostingForm.tsx
src/pages/OpportunityDetail.tsx
src/pages/admin/PostingDetail.tsx

# Enhancement: TPO-admin Student Details popup now shows full Employment records (read-only).
# The "Skills and Employment" card only showed summary employment metrics (first company/designation, count,
# currently-working). Renamed it to "Skills" (dropped the 4 employment summary items) and added a dedicated
# "Employment" section that lists each CurrentEmployment entry as a read-only card (mirrors StudentEmploymentTab
# minus Close/Delete): company + Active/Closed badge, designation · type · package (formatLPA), "Closed on" date
# for closed entries, and document links — Offer letter (offer_letter_url) + Completion proof (completion_proof_url,
# closed only). Empty-state when no records. Backend mapAdminStudent now also returns offer_letter_url +
# completion_proof_name on each employment (columns already existed — additive payload, no migration). Restart BE.
docs/silveroak_backend/src/modules/admin/admin.service.ts
src/types/admin.ts
src/components/admin/AdminStudentDetailsDialog.tsx

# Fix (follow-up): the live admin "Student Details" popup is rendered INLINE in StudentListTab.tsx
# (its own <Dialog>, opened by the row "View"), NOT AdminStudentDetailsDialog.tsx (which is unused/dead).
# Added the same read-only Employment section + trimmed "Skills" card there so the records actually show.
# selectedStudent comes from the list row, but the list + detail share mapAdminStudent, so employments[]
# (incl. offer_letter_url/completion_proof_name) is already in the list payload — no extra backend change.
src/components/admin/StudentListTab.tsx

# Enhancement: Master Data → Posting Types now lists the companies that have used each posting type.
# Under each posting_type master option (TPO admin), show the distinct company names that have had a
# PUBLISHED or CLOSED posting of that type (drafts excluded). Backend getAdminMasters runs one extra query
# (only when posting_type masters are present): prisma.posting.findMany filtered by tenant + the posting_type
# ids + status in [published,closed], distinct on (posting_type_master_id, company_id), grouped into a
# companies[] attached per type via mapMasterOption (companies field set ONLY for posting_type). FE
# ApiMasterOption gains optional companies?: {id,name}[]; MasterDataManagement renders them as chips under
# each posting type ("No companies yet." when none). Read-only aggregation — no schema/migration. Restart BE.
docs/silveroak_backend/src/modules/masters/masters.service.ts
src/types/master.ts
src/pages/admin/MasterDataManagement.tsx

# Enhancement: Posting-type masters gain an Academic Year scope + an academic-year filter (TPO admin).
# Added `target_academic_years` to posting-type master options: the UserScopeSelector ("Institute/Course/Branch
# Scope" card) now has an "Academic Years" multi-select (options from the academic_year master via
# useMasterValues('academic_year'), mirrors the Semester block); threaded through create/edit (TargetScopeState,
# createEmptyTargetScope, openEditDialog, both UserScopeSelector bindings — payloads already spread the scope
# object) + backend (masters.schema create/update + .refine, masters.service MasterOptionWithTargets/mapMasterOption/
# createMaster/updateMaster, normalized via uniqueNormalizedValues). The posting-type row shows an "Academic Year:"
# line, and the Posting Types section has an "Filter by academic year" Select (client-side over the loaded items):
# untagged types (empty target_academic_years = all years) always show; year-tagged types show only for their year.
# ⚠ IMPORTANT: target_academic_years is ADMIN-ONLY scope/label/filter — deliberately NOT added to
# matchesStudentTargeting/StudentTargetValues (students have no academic_year, so gating on it would hide these
# posting types from students). Don't add it to student targeting unless students get an academic_year context.
# ⚠ MIGRATION REQUIRED: schema.prisma — add `MasterOption.target_academic_years String[] @default([])` (additive,
# auto-migratable). Run `npx prisma migrate dev` then restart BE.
docs/silveroak_backend/prisma/schema.prisma
docs/silveroak_backend/src/modules/masters/masters.schema.ts
docs/silveroak_backend/src/modules/masters/masters.service.ts
src/types/master.ts
src/components/admin/UserScopeSelector.tsx
src/pages/admin/MasterDataManagement.tsx

# Bugfix: Branch-scoped posting types vanished for students — even students of that branch.
# Root cause: the student Dashboard surfaces postings via "register interest → matching postings"
# (InterestPostingsCard shows a posting only if its type matches a registered interest), and the
# registerable types come from GET /masters?category=posting_type, which getMasters filters by
# matchesStudentTargeting (AND across institute/course/branch/semester). A student's "branch"
# (Student.department) is course-derived, while the scope picker offers granular CRM branch names —
# so a branch selection never matches a student and, via the AND, cancels the otherwise-correct
# institute/course match, hiding the type (and its postings). Institute/course/semester scope work.
# Fix (user choice "branch falls back to course"): added matchesStudentTargetingForMaster in
# student-targeting.ts — same as matchesStudentTargeting but the branch dimension falls back to the
# branch's parent course (target_courses if course-scoped, else student.course vs the branch label);
# getMasters now uses it. Scoped to master visibility ONLY — postings/applications/analytics keep the
# strict matchesStudentTargeting. Backend-only, NO migration, NO frontend change.
docs/silveroak_backend/src/shared/utils/student-targeting.ts
docs/silveroak_backend/src/modules/masters/masters.service.ts

# Enhancement: Event "pipeline" targeting — send a drive to an institute/course/branch only (TPO admin).
# Create/Edit Event now has an Institute/Course/Branch ("Pipeline") scope card (reuses UserScopeSelector).
# When a pipeline is set, the matching students are auto-assigned to the event (EventStudent rows) so they
# receive it via the existing visibility + publish-notify flow; empty pipeline = manual assignment as before.
# Backend: Event gains target_institutes/target_courses/target_branches; createEvent persists them + resolves
# the pipeline to students and bulk-assigns (createMany skipDuplicates); updateEvent re-resolves ADD-ONLY when
# a target field is in the payload (never removes assignments/attendance/manual rows). Matching uses
# matchesStudentTargetingForMaster (branch falls back to course). Resolution is best-effort (try/catch+logger).
# Frontend: CreateEventInput/ApiEventDetail(+ListItem)/EventFormValues + buildCreateEventPayload +
# eventDetailToFormValues carry the three arrays; EventEditorDialog renders the scope card.
# ⚠ MIGRATION REQUIRED: schema.prisma — Event gains `target_institutes/target_courses/target_branches
# String[] @default([])` (additive, auto-migratable). Run `npx prisma migrate dev` then restart BE.
docs/silveroak_backend/prisma/schema.prisma
docs/silveroak_backend/src/modules/events/event.schema.ts
docs/silveroak_backend/src/modules/events/event.service.ts
src/types/event.ts
src/lib/eventModule.ts
src/components/drives/EventEditorDialog.tsx

# Enhancement (UI): Hide the Student Visibility (institute/course/branch) selector in the Policy dialogs.
# Commented out (not deleted) the PolicyAudienceSelector block + its import + the target* watch vars in
# both Add and Edit Policy dialogs, mirroring how the posting forms hid their UserScopeSelector. The Zod
# fields + handleSubmit still send target_* (as []), so the payload/contract is unchanged — empty targets
# = visible to / required by all students (backend buildStudentAudienceWhere returns {} when empty). The
# Posting Type (global vs linked) selector is separate and untouched. Frontend-only, NO migration.
src/components/admin/AddPolicyDialog.tsx
src/components/admin/EditPolicyDialog.tsx

# Enhancement: Lock student placement re-enable → TPO-admin-only reopen + admin-visible history.
# Students can opt out of placement (global) and per posting type (with reason + confirm) but can NO LONGER
# re-enable themselves — only the T&P office reopens. Enforced server-side (student opt-IN now throws
# PLACEMENT_REOPEN_ADMIN_ONLY) and in the UI (re-enable switches disabled with a "contact T&P" note;
# re-enable dialogs removed). New TPO-admin endpoint PUT /admin/students/:id/placement/reopen
# { scope:'global'|'posting_type', posting_type_master_id? } (no reason) writes an "enabled" history row +
# audit log. The admin Student Details popup gains a Placement card: global opt-out status + reason + date,
# per-type opt-outs, the full enable/disable history with reasons, and Reopen buttons. mapAdminStudent now
# serializes placement_opt_out + posting_type_preferences(opted-out) + placement_pref_history. Apply/interest
# gate messages updated to "contact the T&P office to re-enable." NO migration (all fields/tables already exist).
docs/silveroak_backend/src/modules/students/student.service.ts
docs/silveroak_backend/src/shared/utils/placement-interest.ts
docs/silveroak_backend/src/modules/admin/admin.service.ts
docs/silveroak_backend/src/modules/admin/admin.schema.ts
docs/silveroak_backend/src/modules/admin/admin.controller.ts
docs/silveroak_backend/src/modules/admin/admin.routes.ts
src/components/placement/StudentPlacementTab.tsx
src/components/admin/StudentPlacementSection.tsx
src/components/admin/StudentListTab.tsx
src/types/admin.ts
src/services/adminService.ts
src/hooks/use-admin-api.ts

# Bugfix: Faculty coordinator scoping — Student Directory empty, Institute/Semester dropdowns not loading, NOC not visible.
# Root cause: faculty visibility matched students by exact, case-sensitive `student.department === faculty.department`.
# faculty.department (free-typed or branches[0]) almost never equals the course-derived Student.department, so the
# scoped student set was empty → Directory showed nothing, Institute/Semester dropdown options (derived from that
# empty set) didn't load (Branch still loaded from the master), and the faculty NOC list was empty. The Add User
# institute/course/branch assignment arrays were stored but ignored.
# Fix: new shared/utils/faculty-scope.ts — resolveFacultyScope(user) (normalized union of department+courses+
# branches + institutes) and studentMatchesFacultyScope(student, scope) (tolerant: student department OR course
# matches a target, branch-falls-back-to-course, institute-gated when assigned), reusing normalizeComparable.
# faculty.service.ts now filters students in-memory with this matcher (getStudents/getStudentById/getDashboard) and
# derives Institute + Semester dropdown options from the SCOPED student set (Branch still from master). noc.service.ts
# filters the faculty NOC list/detail in-memory and scope-checks facultyApprove/rejectNoc with the matcher; createNoc
# now notifies faculty whose assignment covers the student. offer.service.ts faculty filter now honours the
# institute/course/branch arrays (exact-match OR, keeps pagination). Backward-compatible (department-only faculty
# still work, now tolerantly). NO migration. NOTE: if Semester still doesn't populate after this, AcademicProfile.semester
# data is unpopulated (real data may live in Student.current_semester) — a separate data fix.
docs/silveroak_backend/src/shared/utils/faculty-scope.ts
docs/silveroak_backend/src/modules/faculty/faculty.service.ts
docs/silveroak_backend/src/modules/noc/noc.service.ts
docs/silveroak_backend/src/modules/offers/offer.service.ts

# Bugfix: NOC "University Placement Drive" category now surfaces the student's TPO-released offers.
# Problem: students placed by the TPO Cell (they have an accepted Offer, not a campus-drive Event) saw an empty
# "Assigned University Drive" dropdown — it was sourced only from Events (useEvents → campus_drive/internship_drive)
# — so they were forced to file the NOC under "By Self" (Self-Sourced). Released offers were not mapped to the
# university/TPO category.
# Fix (frontend-only, NO migration, no backend change): the single required "Assigned University Drive" selector in
# NOCRequestWizard now lists TWO groups — "University Drives" (existing Events) AND "Offers Released by TPO Cell"
# (the student's accepted offers via the existing useMyOffers hook). Option values are prefixed (drive:/offer:) to
# keep the id spaces distinct. Picking an offer sets selected_offer_id, prefills company_name + role_title (and
# program when it is a valid, not-already-issued posting type) — all editable — and submits with
# placement_source='university_drive', drive_id=null. Backend createNoc find-or-creates the company by name (matches
# the offer's existing verified company). Validation requires a drive OR an offer; the assigned-drives path and
# "By Self" path are unchanged.
src/components/noc/NOCRequestWizard.tsx

# Enhancement: TPO Admin Interest Lists — richer table, full Excel export, program dropdown filter.
# Need: Interest Lists (/admin/interests) must surface and let departments export complete student detail
# (Institute, Branch/Course, Semester, registration date) for departmental reporting; the program selector
# was count-cards/badges and the user wants a dropdown filter on top of the table.
# Changes (frontend-only, NO migration — backend already persists InterestRegistration and returns all
# academic fields via mapAdminStudent; endpoint is unpaginated so export is complete):
#  - Program selector replaced with a single <Select> dropdown (label + count) in the filter row, replacing
#    the primary count-cards + secondary badges.
#  - Table now shows Student | Institute | Course | Branch | Semester | CGPA | Registered On | Contact
#    (Registered On resolved per selected program from student.interests[] via the posting-type comparison key;
#    nullable fields fall back to "—").
#  - Export stays .csv (per request) but moved off the hand-rolled `row.join(',')` (6 cols, comma-injection-prone)
#    to a new shared downloadCsvTable() helper that quotes commas/quotes + neutralises formula injection + adds a
#    UTF-8 BOM. Full column set: Name, Enrollment No, Roll No, Institute, Course, Branch, Program, Semester, Batch,
#    CGPA, 10th %, 12th %, Backlogs, Category, Gender, Email, Mobile, Interest Type, Registered On, Profile %.
#  - Bugfix folded in: Department filter options were sourced from an unfiltered registrations query that the
#    backend always returns [] for (no posting_type) → dropdown was always empty. Now derived from a program-only
#    query; department filter resets when the program changes.
src/pages/admin/InterestLists.tsx
src/lib/spreadsheetExport.ts

# Bugfix: TPO Admin verification tab (/admin/students?tab=verification) — Posting Type now shows & filters correctly.
# Problem: the "Posting types" line and the top Posting-Type filter were driven by the student's job applications.
# But verification is a prerequisite to applying, so pending students have NO applications — at most registered
# interests. Result: posting type showed "—" (couldn't tell which program the student enrolled in / showed
# interest in) and the filter returned nobody for pending students.
# Fix:
#  - Frontend (VerificationTab.tsx): getStudentPostingTypeChips() derives posting types from student.interests[]
#    (each carries a human label via INTEREST_LABELS), deduped by label, falling back to application-derived types
#    (formatPostingTypeLabel) for already-applied/re-verification cases. When a student has neither, the row/dialog
#    just show "—" (verification is a post-registration PROFILE check, independent of whether the student has shown
#    interest, so a blank posting type is expected). Row renders chips as secondary Badges; the Review dialog gains
#    a "Posting Types / Interests" section (interest label + registered date). Dropped the unused PostingTypeBadge
#    import. (An earlier program-name fallback was removed — program_name is just the degree level "Under Graduate"
#    for everyone, so it added no value.)
#  - Backend (admin.service.ts): getStudents resolves the posting-type master's value when posting_type_master_id
#    is supplied; buildStudentWhere matches it as an OR of applications.posting.posting_type_master_id OR
#    interest_registrations.interest_type == masterValue (case-insensitive). Inclusive/additive — the shared
#    student list also surfaces interested-but-not-applied students when filtered by posting type.
#    interest_type==master.value is the same assumption the existing InterestLists filter relies on.
# NO migration (query/UI logic only).
src/components/admin/VerificationTab.tsx
docs/silveroak_backend/src/modules/admin/admin.service.ts

# Enhancement: Faculty Coordinator "My Programs" — posting types under their scope + enrolled/interested students + CSV.
# Need: faculty must see the Programs (Posting Types) that fall under their remit and, under each, the students
# enrolled in OR who have shown interest, with full academic detail + CSV export for Academic Heads.
# Design (NO MIGRATION — mapping is DERIVED, no new assignment field): a faculty's programs = active posting-type
# masters whose target_institutes/courses/branches (set by TPO admin at /admin/masters) OVERLAP the faculty's
# existing institutes/courses/branches/department scope. Students under a program = faculty's already-scoped
# students (studentMatchesFacultyScope, unchanged) who are ENROLLED (course/branch under the posting type's target
# scope, via matchesStudentTargetingForMaster) OR INTERESTED (interest_registration interest_type == master value),
# de-duplicated with a source = enrolled|interest|both per row.
# Backend (faculty module only): faculty-scope.ts adds facultyMatchesPostingTypeMaster(scope, targets); faculty.service
# adds getAssignedPrograms() + getProgramStudents() (+ studentUnderProgram/mapProgramStudent helpers, reusing
# matchesStudentTargetingForMaster + normalizeComparable); faculty.schema adds queryProgramStudentsSchema; controller +
# routes add GET /faculty/programs and GET /faculty/programs/students?posting_type=<value>&search= (requireRole
# faculty_coordinator + requirePermission students:view). A posting type not mapped to the faculty -> 403
# PROGRAM_NOT_ASSIGNED. Existing Department Students / dashboard endpoints untouched.
# Frontend: new faculty page src/pages/faculty/FacultyPrograms.tsx (mirrors InterestLists — program dropdown with
# counts, search, student table with Enrolled/Interested badge, CSV via downloadCsvTable with full academic columns),
# new route /faculty/programs, sidebar "My Programs" item; facultyService + use-faculty-api + types/faculty additions.
# NOTE: a posting-type master with EMPTY target arrays (no institute/course/branch target) is INTEREST-ONLY —
# studentUnderProgram sets enrolled=false when the master has no targets, so only students who explicitly
# registered interest appear under it (an empty target would otherwise match every scoped student as "enrolled").
# Targeted posting types still include enrolled (course/branch under target) OR interested.
docs/silveroak_backend/src/shared/utils/faculty-scope.ts
docs/silveroak_backend/src/modules/faculty/faculty.service.ts
docs/silveroak_backend/src/modules/faculty/faculty.schema.ts
docs/silveroak_backend/src/modules/faculty/faculty.controller.ts
docs/silveroak_backend/src/modules/faculty/faculty.routes.ts
src/types/faculty.ts
src/services/facultyService.ts
src/hooks/use-faculty-api.ts
src/pages/faculty/FacultyPrograms.tsx
src/App.tsx
src/components/layout/AppSidebar.tsx

# Bugfix: Super Admin → Security & Access Control → Users — "All Users" search box lost focus after one character.
# Root cause: UserManagementTab has an early `if (usersQuery.isLoading && !usersQuery.data) return <PageLoader/>`,
# and useUsers (use-admin-api.ts) had no placeholderData. Typing a char changed the react-query key → new key had
# no cached data → data undefined + isLoading true → the early return swapped the WHOLE tab (search box included)
# for the loader, unmounting the <input> and stealing focus every keystroke (also flashed on filter/page changes).
# Fix (FE-only, no migration): useUsers now sets `placeholderData: keepPreviousData` (TanStack Query v5) so prior
# results stay while a new key fetches → data never goes undefined → the early loader only fires on the very first
# cold load → the search box stays mounted and keeps focus. The inline "Refreshing users..." indicator
# (isFetching && !isLoading) now covers refetch feedback. No-op for the stable-key callers (stat tiles, audit filter).
src/hooks/use-admin-api.ts

# Bugfix: Official internal-staff email domain changed @silveroak.ac.in -> @silveroakuni.ac.in (TPO/Faculty/Management).
# Hard replace: only @silveroakuni.ac.in is accepted when CREATING tpo_admin/tpo_employee/faculty_coordinator/management
# accounts; the old domain is rejected. The sole product-code enforcement is admin.schema.ts createUserSchema.superRefine
# (BE) mirrored by the INSTITUTE_EMAIL_DOMAIN constant in UserManagementTab.tsx (FE, drives the toast/hint/placeholder).
# Adversity notes: LOGIN is NOT domain-gated, so existing @silveroak.ac.in staff keep logging in and new-domain accounts
# already log in — no data migration needed (existing accounts intentionally left untouched). updateUserSchema has no email
# field (nothing to change). prisma/seed.ts inserts via Prisma (bypasses Zod) and is intentionally left on the old domain,
# so all login-based tests stay green. The only tests that CREATE internal staff via POST /admin/users were updated to the
# new domain so the suite stays green: remaining-modules.test.ts (newuser@ as tpo_employee) and admin-crm.test.ts
# (crm.faculty@/crm.tpo@/duplicate.crm@). auth.swagger.ts login example updated (cosmetic). No migration.
docs/silveroak_backend/src/modules/admin/admin.schema.ts
src/components/superadmin/UserManagementTab.tsx
docs/silveroak_backend/src/modules/auth/auth.swagger.ts
docs/silveroak_backend/src/__tests__/remaining-modules.test.ts
docs/silveroak_backend/src/__tests__/admin-crm.test.ts

# Bugfix: Rejected students no longer appear in the Posting Type sections (interest/program listings).
# When a student's verification_status is 'rejected', they must not show in any "students by posting type" view.
# The interest/program queries filtered by interest/scope but never excluded rejected students. Added
# `verification_status: { not: VerificationStatus.rejected }` (VerificationStatus already imported) to the four
# queries: admin getInterestRegistrations (list) + getInterestSummary (dropdown counts, kept consistent with the
# list), and faculty getAssignedPrograms (program counts) + getProgramStudents (students under a program).
# Pending + verified students are unaffected. Student Management lists + Selection Database are unchanged
# (rejected must stay visible there). BE-only, no migration.
docs/silveroak_backend/src/modules/admin/admin.service.ts
docs/silveroak_backend/src/modules/faculty/faculty.service.ts

# Bugfix: Student Management → Verification — re-verify (and change status) after rejection.
# The verification tab hard-coded verification_status:'pending', and the Pending/Verified/Rejected cards were
# counts only, so rejected students were never listed and had no Verify action. The backend verifyStudent has no
# state-machine guard (it sets whatever status is passed) and verifyStudentMutation already supports verified/rejected
# — so this was a pure UI gap. FE-only (VerificationTab.tsx): the 3 count cards are now clickable status filters
# (highlight active); the list uses the selected status. Per-row + Review-dialog actions are gated by status —
# Verify shows unless already verified; Reject shows unless already rejected (pending: both; rejected: Verify to
# re-verify; verified: Reject). Bulk Verify Selected/All + selection checkboxes show only for pending/rejected
# (not verified). Copy generalized (title "<Status> Students", search/empty text). No backend change, no migration.
src/components/admin/VerificationTab.tsx

# Enhancement: Posting-Type policy acceptance enforced before Show Interest / Apply.
# TPO admins can link a policy to a posting type in the Policy Repository (Policy.posting_type_master_id +
# rich content), but linked policies were surfaced to no student. Now, when a student clicks Show Interest or
# Apply for a posting type, a modal shows the policy(ies) linked to that type with an "I Agree" per policy;
# the action proceeds only after ALL linked policies are accepted. Also enforced server-side. NO MIGRATION
# (PolicyAcceptance already keys on (student_id, policy_id, policy_updated_at) — a linked policy's id fits).
# Backend:
#  - policy.schema.ts: queryPoliciesSchema gains posting_type_master_id.
#  - policy.service.ts: buildPolicyWhere returns a posting type's linked policies when posting_type_master_id
#    is passed (students too), else keeps the global-only clamp; getPolicyById lets a student read an
#    audience-visible LINKED policy's content (was global-only).
#  - student.service.ts: acceptPolicy looks a policy up by id via a new buildAudiencePolicyWhere (audience,
#    no global clamp) so a linked policy can be accepted, and ONLY recomputes the global student.policy_accepted
#    boolean for GLOBAL policies — a linked acceptance never flips it (avoids the documented placement deadlock).
#    registerInterests now asserts the policy for each requested posting type.
#  - shared/utils/posting-type-policy.ts (NEW): assertPostingTypePolicyAccepted(studentId, postingTypeMasterId)
#    — no-op when the type has no linked policy; else throws 422 POSTING_TYPE_POLICY_NOT_ACCEPTED unless every
#    audience-visible linked policy has a current acceptance (mirrors placement-interest.ts). Wired into
#    application.service.apply() (after the per-type guards) + student.service.registerInterests().
# Frontend:
#  - types/policy.ts: PolicyQueryParams gains posting_type_master_id. use-policy-api.ts: usePolicies gains an
#    `enabled` flag. GlobalPolicyCard: new defaultExpanded prop (shows content up-front in the gate).
#  - components/policies/PostingTypePolicyDialog.tsx (NEW): fetches the posting type's linked policies; if none
#    or all already accepted it proceeds transparently (no modal), else shows one GlobalPolicyCard per policy
#    (reuses useAcceptPolicy) and enables Continue only once all are accepted.
#  - OpportunityDetail.tsx: the 3 Apply buttons open the policy gate (onProceed -> the existing ApplyDialog).
#  - Dashboard.tsx: the Register (Show Interest) button opens the gate for that posting type (value->master id
#    via usePostingTypeOptions), onProceed -> registerInterests.
docs/silveroak_backend/src/modules/policies/policy.schema.ts
docs/silveroak_backend/src/modules/policies/policy.service.ts
docs/silveroak_backend/src/modules/students/student.service.ts
docs/silveroak_backend/src/modules/applications/application.service.ts
docs/silveroak_backend/src/shared/utils/posting-type-policy.ts
src/types/policy.ts
src/hooks/use-policy-api.ts
src/components/policies/GlobalPolicyCard.tsx
src/components/policies/PostingTypePolicyDialog.tsx
src/pages/OpportunityDetail.tsx
src/pages/Dashboard.tsx

# Enhancement: Restrict posting visibility & apply to students ENROLLED in the posting type
# A published posting was visible to and appliable by every student who could reach the Program, even if
# they never enrolled (Show Interest) in that Posting Type. Now a student only sees / can apply to postings
# under a Posting Type they have registered interest in (or already have an application/offer for — so
# in-flight applications aren't hidden). No migration (reuses InterestRegistration.interest_type = master value).
# Backend:
#  - shared/utils/posting-type-interest.ts (NEW): getRegisteredInterestValues (enrolled set),
#    getVisiblePostingTypeValues (enrolled UNION already-applied/offered types), assertInterestRegistered
#    (apply-path guard, throws BusinessRuleError 'POSTING_TYPE_NOT_ENROLLED' HTTP 422). Mirrors
#    self-placed-noc-block.ts; matches on trimmed/lowercased master value.
#  - modules/postings/posting.service.ts: getStudentTargetContext now also returns the studentId; the student
#    branches of getPostings + getPostingById filter/hide postings whose posting type isn't in the student's
#    visible set (getPostingById throws NotFoundError, same as a targeting miss). Non-student paths untouched.
#  - modules/applications/application.service.ts: apply() calls assertInterestRegistered before the other
#    per-type guards (after the global policy check). Backstop for the visibility gate.
# Frontend:
#  - pages/Opportunities.tsx: zero-postings empty state now explains enrollment and links to the Dashboard
#    ("Register interest on Dashboard"). Enforcement is backend-driven, so lists auto-filter.
docs/silveroak_backend/src/shared/utils/posting-type-interest.ts
docs/silveroak_backend/src/modules/postings/posting.service.ts
docs/silveroak_backend/src/modules/applications/application.service.ts
src/pages/Opportunities.tsx

# Enhancement: Simplify Create Event — driven by Posting Type, audience inherited
# The Create Event form no longer needs a manual audience (Pipeline) config and no longer lists all
# companies. Flow is now: pick Posting Type -> Company dropdown shows only companies with a posting of that
# type -> Linked Posting shows only that company's postings of that type -> Institute/Program/Branch are
# auto-inherited from the Posting Type (Semester/Academic Year shown read-only). Event Type stays a separate
# required field. FRONTEND-ONLY, NO MIGRATION — Event already has company_id/posting_id/target_* columns and
# the backend already accepts them + runs pipeline auto-assignment off target_*.
#  - hooks/use-posting-type-options.ts: PostingTypeOption now also carries the master's target_institutes/
#    courses/branches/semesters/academic_years (read off ApiMasterOption; public /masters already returns them).
#  - lib/eventModule.ts: EventFormValues gains form-only posting_type_master_id (not a persisted column);
#    createEmptyEventFormValues + eventDetailToFormValues updated (edit prefills posting type best-effort from
#    the linked posting's type value). buildCreateEventPayload shape unchanged (still sends company_id/
#    posting_id/type/target_institutes/courses/branches — now the inherited values).
#  - components/drives/EventEditorDialog.tsx: added Posting Type Select (first cascade field); Company options
#    derived as distinct companies from usePostings({posting_type_master_id}); Linked Posting filtered from the
#    same list by company; removed UserScopeSelector (Pipeline) in favour of a read-only "Audience (inherited
#    from Posting Type)" summary; submit now also requires posting_type_master_id. Dropped the useCompanies import.
src/hooks/use-posting-type-options.ts
src/lib/eventModule.ts
src/components/drives/EventEditorDialog.tsx

# Enhancement: Create Event — Linked Posting is now MULTI-select (associate one event with multiple roles)
# ⚠ MIGRATION REQUIRED: schema.prisma — Event.posting_ids String[] @default([]) (additive; new column only,
#   no FK/data change). Run: cd docs/silveroak_backend && npx prisma migrate dev  (prod: npx prisma migrate deploy)
# Additive legacy-mirror pattern: posting_ids holds all linked postings; the existing single posting_id +
# `posting` relation are KEPT as a back-compat mirror = posting_ids[0], so single-posting consumers
# (DrivesManagement badge, EventAssignmentDialog candidate source, GenerateCircularDialog, posting_type filter,
# notifications) keep working unchanged. Visibility is UNCHANGED (events show via EventStudent assignment scoped
# by company_id + inherited pipeline target_*; linked postings are associative only).
# Backend:
#  - prisma/schema.prisma: Event.posting_ids String[] @default([]) (mirror posting_id kept).
#  - modules/events/event.schema.ts: createEventSchema gains posting_ids (array of uuid, default []).
#  - modules/events/event.service.ts: createEvent writes posting_ids + mirror posting_id = posting_ids[0];
#    updateEvent keeps the mirror in sync; new attachEventPostings() adds postings:[{id,title,type}] to the
#    list + detail responses (batched findMany) while still emitting the legacy single `posting`.
# Frontend:
#  - types/event.ts: ApiEventListItem/ApiEventDetail gain posting_ids + postings[]; CreateEventInput gains
#    posting_ids? (posting_id kept optional).
#  - lib/eventModule.ts: EventFormValues uses posting_ids[]; buildCreateEventPayload sends posting_ids;
#    eventDetailToFormValues hydrates posting_ids (falls back to [posting_id]); search fields include all roles.
#  - components/drives/EventEditorDialog.tsx: Linked Posting is now a SearchableMultiSelect; resets on posting
#    type/company change; edit resolves posting type from the first linked posting.
#  - pages/admin/DrivesManagement.tsx: renders a badge per linked role (all postings), not just the first;
#    dropped the now-unused formatPostingTypeLabel import.
#  - components/drives/EventDetailDialog.tsx: added a "Linked Roles" section listing all linked postings.
docs/silveroak_backend/prisma/schema.prisma
docs/silveroak_backend/src/modules/events/event.schema.ts
docs/silveroak_backend/src/modules/events/event.service.ts
src/types/event.ts
src/lib/eventModule.ts
src/components/drives/EventEditorDialog.tsx
src/pages/admin/DrivesManagement.tsx
src/components/drives/EventDetailDialog.tsx

# Enhancement: NOC wizard — rename "Assigned University Drive" -> "Released Offers by TPO" (offers-only)
# The NOC Request wizard's placement selector (shown for placement_source = university_drive) is renamed and
# now lists ONLY the signed-in student's own released offers. FRONTEND-ONLY, NO migration — GET /offers/my
# (useMyOffers) is already student-scoped and returns all statuses; the offer selection only prefills
# company/role (nothing new persisted).
#  - Label "Assigned University Drive" -> "Released Offers by TPO".
#  - Offers filter broadened from status==='accepted' to released-but-not-rejected
#    (pending_student_action + accepted); memo renamed acceptedOffers -> releasedOffers.
#  - Removed the "University Drives" SelectGroup and all now-dead drive plumbing (useEvents/drivesQuery,
#    availableDrives, selectedDrive, the drive company-name prefill effect, the drive branch of
#    handlePlacementSelectionChange, and the SelectGroup/SelectLabel/useEvents imports). drive_id form/payload
#    plumbing kept (create still sends drive_id: null for university_drive).
#  - Copy updated (placeholder/loading/empty-state); validation now requires a selected offer
#    ("Please select a released offer.").
src/components/noc/NOCRequestWizard.tsx

# Bugfix + Enhancement: Placed Students Database — fetch released offers + NOC Status label
# Path: Student Management -> Selection Database -> Placed Students Database (GET /admin/selection-database).
# FRONTEND + BACKEND, NO migration (noc_status is computed at query time).
# Issue 1 (bug): the placements query filtered offers to type:'job' AND OR[status='accepted',
#   joining_status IN (joined, did_not_join)], so RELEASED offers (created as status=pending_student_action,
#   joining_status=pending) matched nothing -> zero rows. Fixed by filtering to all non-rejected offers
#   (status NOT IN rejected_by_admin/rejected_by_student), so placed students appear on offer release.
# Issue 2 (enhancement): each record now carries noc_status ('issued' | 'pending') — 'issued' when the
#   student has a NocRequest with status='issued', else 'pending' — rendered as a NOC Issued / NOC Pending
#   badge column (and in the CSV export).
# Backend (admin.service.ts getSelectionDatabase): broadened offer where-clause; one batched
#   prisma.nocRequest.findMany({status:'issued', student_id in [...]}) -> issuedSet; noc_status added to the
#   placement + internship mapped rows and to the applySelectionFilters inline row type.
# Frontend: types/admin.ts ApiAdminSelectionRecord += noc_status; SelectionDatabaseTab.tsx local
#   SelectionRecord += noc_status, new NocStatusBadge + "NOC Status" column (after Outcome), colSpan 11->12,
#   CSV header/row added.
docs/silveroak_backend/src/modules/admin/admin.service.ts
src/types/admin.ts
src/components/admin/SelectionDatabaseTab.tsx

# Bugfix/Enhancement: NOC Program list is per placement-source (not hidden by posting-type targeting)
# The NOC wizard's "Program / Category" list was fed by GET /masters?category=posting_type, which for a
# student is filtered by posting-type TARGETING (matchesStudentTargetingForMaster) — so a placed student
# whose posting-type master isn't scoped to their institute/course/branch saw an EMPTY Program list and
# couldn't request a NOC, even though createNoc accepts any active posting type. Now the Program list is
# scoped to how the student was PLACED, per placement source:
#   - University Placement Drive -> only posting types the student has an OFFER for (from useMyOffers).
#   - Self-Sourced -> ALL ACTIVE posting types (targeting bypassed).
# To pick source before program, the Placement Source radio was moved from step 2 to the TOP of step 1
# (NOC_STEP_FIELDS: step1=['placement_source','program'], step2 loses placement_source; step titles tweaked).
# NO migration. Targeting is unchanged everywhere else (Dashboard interest, Opportunities filter).
# Backend:
#  - masters.schema.ts: queryMastersSchema gains all_targets (z.string -> === 'true'; opt-in, safe boolean).
#  - masters.service.ts: getMasters skips the student-targeting filter when all_targets is set.
# Frontend:
#  - types/master.ts: MasterQueryParams gains all_targets?: boolean.
#  - hooks/use-master-api.ts: useMasterValues(category, enabled, allTargets) forwards all_targets + adds it to
#    the query key (masterKeys.publicValues). Backward-compatible (default false).
#  - components/noc/NOCRequestWizard.tsx: postingTypeValuesQuery uses all_targets=true (all-active list for
#    Self-Sourced); new offerPostingTypeValues (placed types) + activePostingTypeValues switch by
#    form.placement_source; Placement Source radio moved into step 1 (resets program on change); step titles.
docs/silveroak_backend/src/modules/masters/masters.schema.ts
docs/silveroak_backend/src/modules/masters/masters.service.ts
src/types/master.ts
src/hooks/use-master-api.ts
src/components/noc/NOCRequestWizard.tsx

# Bugfix: Auto-create + link a Recruiter when a NOC's NEW company is approved by TPO
# When a student self-sources a NOC with a new company, createNoc already creates the Company
# (source=student, pending) and TPO approval verifies it — but no Recruiter was created, so the new
# employer had no linked recruiter/contact. Now, on TPO approval, a Recruiter is auto-created from the NOC's
# contact person and linked to the (new/student-sourced) company. RECORD-ONLY (no login User/password — this
# system has no email transport; an admin can provision a login later via the existing recruiter flow, which
# re-links a user_id-null recruiter). BACKEND logic + FE/BE validation; NO migration.
#  - noc.service.ts: new best-effort createRecruiterForApprovedNoc(noc, userId) (mirrors markLinkedCompanyVerified's
#    try/catch); guards source==='student' + zero-existing-recruiters + name/email present (idempotent, unique-safe);
#    creates Recruiter (user_id null, verification_status=verified). Wired right after markLinkedCompanyVerified in
#    BOTH tpoApprove and issueNoc. Imports VerificationStatus.
#  - noc.schema.ts: createNocSchema.superRefine now REQUIRES contact_person_name + contact_person_email when
#    placement_source === 'self_sourced' (so a recruiter can always be built/linked). university_drive unaffected.
#  - NOCRequestWizard.tsx: validateNocForm requires contact person name + email for self_sourced; those two
#    field labels wrapped in RequiredLabel (self-sourced render only).
docs/silveroak_backend/src/modules/noc/noc.service.ts
docs/silveroak_backend/src/modules/noc/noc.schema.ts
src/components/noc/NOCRequestWizard.tsx

# Bugfix: No Dues (and all) uploaded attachment links open a blank/404 page — "not visible"
#  Root cause: attachments are stored as root-relative `/uploads/...` paths and the shared
#  resolveBackendAssetUrl returned them unchanged, so the browser resolved them against the
#  FRONTEND origin. In a single-origin reverse-proxy prod (frontend + `/api`→backend on one host),
#  only `/api` is forwarded to the backend, so `/uploads/x` 404s. proof_url persist/return/render
#  are all correct — the break is purely file-serving/URL-resolution. Shared resolver → the fix
#  repairs every attachment type (No Dues, NOC, policies, internships, portfolio, resumes,
#  announcements, profile photos). No migration; backward compatible (existing /uploads mount kept).
#  - app.ts: added a second PUBLIC static mount `app.use('/api/uploads', express.static(uploadDir))`
#    placed ABOVE the `/api` auth middleware, so uploads are reachable via the proxied /api path.
#  - studentModule.ts: resolveBackendAssetUrl now prefixes `/uploads/...` paths with the API base
#    (origin + `/api` prefix) instead of returning them relative to the frontend origin; external
#    http(s) URLs and non-upload values are unchanged.
docs/silveroak_backend/src/app.ts
src/lib/studentModule.ts

# Bugfix: Posting Operations — Posting Type filter missing/non-functional + search box loses focus
#  1. The visible "Type" dropdown sent `type=<value>`, which the backend queryPostingsSchema does not
#     accept (Zod strips it; getPostings never filters on it) — so it did nothing. Replaced it with a
#     working "Posting Type" filter driven by usePostingTypeOptions() that sends `posting_type_master_id`
#     (UUID), which the backend supports. SelectItem value bound to option.id (not value).
#  2. Search box deselected after 1 char: usePostings had no keepPreviousData, so each keystroke made a
#     new query key -> postingsQuery.isLoading true -> the page's early skeleton return unmounted the
#     <Input>. Added `placeholderData: keepPreviousData` to usePostings (mirrors the useUsers fix).
#  Frontend-only; no backend/schema/migration change.
src/hooks/use-posting-api.ts
src/pages/admin/PostingsManagement.tsx

# Bugfix: Notification module crashed with React error #130 (Element type is undefined)
#  Root cause: NotificationsDropdown's notificationIcons map only covered 4 of the 13
#  NotificationType values, so any modern notification (offer/application/interest/noc/event/
#  announcement/circular/no_dues/recruiter) resolved `Icon` to undefined and `<Icon/>` threw #130,
#  crashing the bell dropdown. Completed the icon map for all 13 types (typed as
#  Record<NotificationType, LucideIcon> so a missing key is now a compile error) and added a
#  `?? Bell` runtime fallback for any unknown/future type. Frontend-only; no backend/migration.
src/components/layout/NotificationsDropdown.tsx

# Bugfix: Notification window can't scroll
#  The shadcn/Radix ScrollArea viewport uses height:100% (h-full), which only resolves against a
#  parent with a definite height; the ScrollArea root had only max-h-[420px] (height:auto), so the
#  viewport grew to full content height and the root clipped it (overflow-hidden) with no scroll.
#  Replaced the ScrollArea with a native `max-h-[420px] overflow-y-auto` div, which scrolls
#  correctly with a max-height. Frontend-only.
src/components/layout/NotificationsDropdown.tsx

# Bugfix: Missing Profile Photo Update Option in Student Dashboard
#  The student profile photo upload flow already existed but was only reachable via a plain file
#  input buried in the Profile page's Personal tab (and the access gate, which only fires when a
#  student has NO photo). Once a student had any photo, there was no discoverable way to change it —
#  the prominent avatars (Profile header, Dashboard, sidebar) had no click/edit affordance. Made the
#  Profile page header avatar a clickable "change photo" control with a camera badge that triggers
#  the existing hidden file input -> crop/preview dialog -> upload (reuses profilePhotoInputRef,
#  handleUploadProfilePhoto, useUploadStudentProfilePhoto; POST /students/me/profile-photo). No new
#  state/handlers/hooks. Frontend-only; no backend/schema/migration change.
src/pages/Profile.tsx

# Bugfix: Uploaded profile photo not shown on the top-bar profile chip (UserMenu)
#  Follow-up to the profile-photo discoverability fix. Upload worked and the image was served fine
#  (200 image/png via /api/uploads), and the Dashboard/Sidebar/Profile-header avatars already render
#  it, but the top-bar profile chip (UserMenu, shown on every page) was structurally initials-only:
#  it had no AvatarImage and read from useAuth().user, which carries no profile_photo_url. Added an
#  AvatarImage sourced from the student profile via the same role-gated pattern the sidebar uses
#  (useStudentProfile(currentRole === 'student') + resolveBackendAssetUrl over the shared
#  studentKeys.profile() cache, so it auto-refreshes on upload). Non-students still get initials.
#  Frontend-only; no backend/schema/migration change.
src/components/layout/UserMenu.tsx

# Bugfix: Profile photos never rendered in the browser (initials shown everywhere)
#  Uploads persisted and served fine (curl -> 200 image/*), and the read path/types/resolver/avatars
#  were all correct, but NO avatar image rendered in the browser. Root cause: helmet()'s default
#  Cross-Origin-Resource-Policy: same-origin was applied to the /uploads static responses, so the
#  browser BLOCKED cross-origin <img> loads (dev frontend :8080 embedding images from the API :3000;
#  also any split-origin/CDN prod). curl ignores CORP, which is why the same URL returned 200 on the
#  CLI but showed blank in the app (DevTools: fallback span, no <img>). Fix: serve the two public
#  upload static mounts (/uploads and /api/uploads) with Cross-Origin-Resource-Policy: cross-origin
#  via a shared express.static setHeaders handler; the global helmet() default stays strict for every
#  other route. Topology-universal: safe/additive for single-origin prod, and fixes split-origin/CDN.
#  No migration; backend rebuild + restart to ship (CORP is a response header - hard-refresh / purge
#  CDN for /uploads if a stale same-origin header was cached).
docs/silveroak_backend/src/app.ts

# Bugfix: Create Event – Target Audience ignored Company/Role (assigned the whole Posting Type)
#  When creating an event (Posting Type -> Company -> Job Profile/Role), the auto-assigned target
#  audience was every student in the posting type's broad institute/course/branch scope: the backend
#  resolver (event.service.resolvePipelineStudentIds) loaded ALL tenant students and matched them
#  against the event's target_* arrays (seeded from the posting-type master in EventEditorDialog's
#  handlePostingTypeChange), completely ignoring the selected company_id and posting_ids that were
#  already stored on the event. Fix: audience is now the DISTINCT students who APPLIED to the selected
#  role(s) via the Application table (resolveApplicantStudentIds: posting_id IN posting_ids; when no
#  role is selected, fall back to applicants across the company's postings). createEvent/updateEvent
#  now pass company_id + posting_ids and re-resolve (add-only, never removes) when the company or
#  linked role(s) change. Frontend: EventEditorDialog stops seeding target_* from the posting-type
#  master and the "Audience (inherited from Posting Type)" preview (institute/program/branch/semester
#  chips) is replaced with an accurate applicant-based summary keyed off the selected company + roles.
#  Event.target_* columns are now vestigial (kept for back-compat, not dropped). No migration
#  (reuses the existing Application table + Event.company_id/posting_ids).
docs/silveroak_backend/src/modules/events/event.service.ts
src/components/drives/EventEditorDialog.tsx

# Bugfix: ERP-fetched user fields must be read-only (Add User, Edit User, self-profile) for every role
#  In Super Admin -> Add User, fields autofilled from the ERP/CRM (employee lookup: name, email, phone,
#  department, designation + crm_employee_code marker) were fully editable; Edit User left name/phone/
#  department/designation editable for CRM-linked users; and staff My Profile let ERP-linked staff edit
#  their phone/designation. A user is ERP-linked when crm_employee_code is non-null (students are already
#  locked via Profile.tsx ReadonlyField + updatePersonal .strict()). Fix:
#  Frontend:
#   - UserManagementTab Add User: name/email/phone/designation/department inputs are readOnly (bg-muted)
#     when newUser.crm_employee_code is set (ERP-fetched); CRM autofill copy updated to say fields are
#     read-only.
#   - UserManagementTab Edit User: same four inputs readOnly when editUser.crm_employee_code is set
#     (email already read-only) + an ERP note.
#   - MyProfile: ERP-linked staff see phone + designation as read-only (Edit hidden) with a note; password
#     + notifications unchanged. Non-ERP staff keep editable phone/designation.
#   - authService AuthUser type gains crm_employee_code.
#  Backend (authoritative enforcement so no role can bypass via API):
#   - auth.service meSelect returns crm_employee_code; updateMe throws 422 ERP_MANAGED_FIELD for
#     ERP-linked users (phone/designation are ERP-owned).
#   - admin.service updateUser drops ERP-owned fields (name/phone/department/designation/crm_employee_code)
#     from the write for ERP-linked users; role/scope/is_active stay editable.
#  No migration (crm_employee_code column + DTO fields already exist).
src/components/superadmin/UserManagementTab.tsx
src/pages/shared/MyProfile.tsx
src/services/authService.ts
docs/silveroak_backend/src/modules/auth/auth.service.ts
docs/silveroak_backend/src/modules/admin/admin.service.ts

# Bugfix: Project date validation in Student Profile -> Projects
#  Project Start/End dates were under-validated: the inline project editor in Profile.tsx only checked
#  presence (start required; end required when not ongoing) with no future-date guard and no start<=end
#  ordering; the date inputs had no max; and the backend project schema had no future/ordering refine
#  (updateProjectSchema was .partial(), dropping validation). Fix:
#   - Profile.tsx: added a memoized local todayIso; date pickers now have max={today} (start & end) and
#     the End picker has min={start}; handleSaveProject rejects a future start, a future end, and
#     start>end with clear toasts (equal dates allowed; ongoing projects skip end checks).
#   - student.schema.ts (backend, defense-in-depth): shared refineProjectDates (no future dates; start
#     must not be after end) wired into BOTH createProjectSchema (alongside the existing
#     end-required-if-completed check) and updateProjectSchema.
#  No migration.
src/pages/Profile.tsx
docs/silveroak_backend/src/modules/students/student.schema.ts

# Bugfix: Certification date validation missing (future issue dates allowed)
#  In Student Profile -> Certifications, the Issue Date input accepted future dates. The inline cert
#  form in Profile.tsx (create-only) had no date validation and the date input had no max; the backend
#  createCertificationSchema had no future guard on issue_date. Fix: added max={todayIso} on the Issue
#  Date picker, a guard in handleAddCertification (reject issue_date > today with a toast; issue_date
#  stays optional), and a .superRefine on createCertificationSchema rejecting a future issue_date.
#  No migration.
src/pages/Profile.tsx
docs/silveroak_backend/src/modules/students/student.schema.ts

# Bugfix: Student Portfolio not displaying Project Start/End dates (TPO/Admin Student Details popup)
#  The admin Student Details popup's "Projects" card (StudentListTab.tsx) rendered title/description/
#  technologies/GitHub/Demo but omitted the project start/end dates, even though the backend admin
#  mapping (mapAdminStudent) already returns start_date/end_date/is_ongoing and the data is populated.
#  (Other portfolio surfaces -- student /portfolio, recruiter PortfolioReadOnlyView, and the popup's
#  separate "Portfolio Projects" card -- already showed them.) Fix: added a date-range line to the
#  "Projects" card reusing the existing local formatDateRange() helper + an Ongoing badge for parity
#  with the "Portfolio Projects" card. Frontend-only; no backend/schema/migration change.
src/components/admin/StudentListTab.tsx

# Bugfix: Student -> Opportunities Minimum Stipend filter had no effect
#  The Opportunities page filters client-side; the Minimum Stipend slider is in thousands
#  (₹{minStipend}K/month, 0-50) but was compared directly against a raw rupee amount
#  (stipendValue < filters.minStipend), so e.g. 25000 < 40 was never true and nothing filtered.
#  Also, extractCompensationNumber's regex stopped at the comma, so "10,000 - 20,000 / month" parsed
#  to 10 instead of 10000. Fix: extractCompensationNumber now strips thousands separators before
#  matching (CTC "3 - 6 LPA" unaffected, no commas); the stipend comparison multiplies the slider value
#  by 1000 to compare in rupees. Lower-bound (first number) semantics kept; no-stipend postings still
#  hidden when a minimum is set. Frontend-only; no backend/schema/migration.
src/lib/postingModule.ts
src/pages/Opportunities.tsx

# Bugfix: TPO Admin NOC review didn't show the approving faculty's name (only date/time)
#  NOCReviewDialog (TPO admin review, uses the detail endpoint which already returns
#  faculty_approved_by_user.name) rendered <NOCStatusTimeline> WITHOUT the facultyApproverName /
#  tpoApproverName props, so the timeline fell back to "Approved on <date>" instead of "Approved by
#  <name> on <date>". Passed both names via the existing getNocFacultyApproverName /
#  getNocTpoApproverName helpers (mirrors NOCRequestDetailSheet). Frontend-only, single file; the
#  timeline component + helpers + detail API already supported names. No backend/schema/migration.
src/components/noc/NOCReviewDialog.tsx

# Bugfix: Attachment viewing gaps + restrict document attachments to PDF-only
#  Part 1 (viewing): the student NOC detail sheet opened offer-letter/supporting-document links from the
#  RAW stored /uploads path (bypassing resolveBackendAssetUrl -> 404 in split-origin/proxy deploys);
#  wrapped both in resolveBackendAssetUrl (matches the admin NOCReviewDialog). Added an "Open certificate
#  in a new tab" fallback link under the two cross-origin PDF <iframe> previews (NOCRequestDetailSheet,
#  NocCertificate) which helmet CORP/X-Frame-Options blocks in split-origin. (The broad "not viewable
#  across modules" symptom is otherwise the earlier /api/uploads + CORP: cross-origin + resolver fixes
#  needing deployment - backend-first.)
#  Part 2 (PDF-only): per decision, restricted DOCUMENT attachments to PDF-only while keeping images for
#  announcement + portfolio/internship proofs. Backend upload.ts: policyDocumentUpload, nocOfferLetterUpload,
#  nocSupportingDocumentUpload, noDuesProofUpload, certificationDocumentUpload, employmentDocumentUpload now
#  use PDF_MIME_TYPES ("Only PDF files are allowed"); shared RESUME_MIME_TYPES/SUPPORTING_DOCUMENT_MIME_TYPES
#  constants untouched (still used by internship/portfolio/announcement). Frontend: tightened the matching
#  file inputs' accept to .pdf,application/pdf + updated helper text/type-guard consts. No migration
#  (existing non-PDF uploads remain viewable).
docs/silveroak_backend/src/middleware/upload.ts
src/components/admin/PolicyDocumentUploadField.tsx
src/components/noc/NOCRequestWizard.tsx
src/components/noc/NOCRequestDetailSheet.tsx
src/pages/NocCertificate.tsx
src/pages/Profile.tsx
src/pages/NoDuesCertificate.tsx
src/components/employment/EmploymentCloseDialog.tsx
src/components/employment/StudentEmploymentTab.tsx

# Bugfix: Create Event — dropdown search inconsistency
#  In the Create/Edit Event dialog the Company field was a searchable combobox (shared
#  SearchableSelect) while Event Type and Posting Type were plain shadcn <Select> dropdowns with no
#  search box — inconsistent within one form and painful once admins add many event_type/posting_type
#  masters. Swapped both to the existing SearchableSelect (already imported in this file for Company),
#  keeping every binding intact: Event Type still binds option.value (stored on Event.type) and Posting
#  Type still binds option.id (the UUID sent as posting_type_master_id) — the recurring id-vs-value
#  gotcha. handlePostingTypeChange is unchanged, so picking a posting type still clears company_id /
#  posting_ids / target_*. No clearable option added (both fields required, matching prior behaviour);
#  the "No event/posting types configured…" helper texts and disabled/loading states are preserved.
#  Removed the now-unused Select* import. Frontend-only, one file; no backend/schema/migration.
src/components/drives/EventEditorDialog.tsx

# Bugfix: Master Data — "Referenced record does not exist" when deleting some posting types
#  Root cause (two layers): (1) Posting.posting_type_master_id is onDelete: Restrict, so Postgres
#  refuses to delete a posting_type master while ANY posting (draft/published/closed) references it —
#  that's why only *some* posting types failed; (2) deleteMaster did no dependency pre-check, so the
#  raw Prisma P2003 hit the generic error-handler branch whose message ("Referenced record does not
#  exist") is worded for the create-with-bad-FK case and is actively misleading for a blocked delete.
#  Fix: deleteMaster now pre-counts postings for posting_type masters and throws ConflictError 409
#  'MASTER_OPTION_IN_USE' naming the count and pointing at the existing Deactivate action. The generic
#  P2003 message was reworded to cover both directions (status 400 + code FOREIGN_KEY_VIOLATION kept —
#  no contract change). getAdminMasters additionally returns per-posting-type dependent counts
#  (usage{postings,noc_templates,student_preferences,policies}) via 4 groupBy queries scoped to the
#  posting-type ids only (mirrors the existing companies[] pass — deliberately NOT a per-row _count
#  over every master category); the student-facing getMasters passes no counts, so `usage` is
#  admin-only. FE: the delete confirmation now spells out the consequences that were previously
#  silent — NOC templates and student placement-preference rows cascade-delete, linked policies fall
#  back to global — and shows the blocking line when postings exist. Delete button intentionally left
#  ENABLED (server 409 stays the source of truth). Bullets are spans, not <ul>, because
#  AlertDialogDescription renders a <p>. Added 2 backend tests (409 on in-use type; usage present for
#  admin + absent for students). No schema change, NO MIGRATION.
docs/silveroak_backend/src/modules/masters/masters.service.ts
docs/silveroak_backend/src/middleware/error-handler.ts
docs/silveroak_backend/src/__tests__/masters.test.ts
src/types/master.ts
src/pages/admin/MasterDataManagement.tsx

# Bugfix: Announcements — multi-select Institute/Course/Branch + hierarchical Semester targeting
#  Three independent layers were broken: (1) BE Zod capped target_institutes/courses/branches at
#  .max(1), so only ONE of each could ever be saved; (2) the form used PolicyAudienceSelector, three
#  SINGLE SearchableSelects that read targets[0] and write [name]; (3) Semester existed in the DB
#  (Announcement.target_semesters) but was only exposed under the "Specific Semester" audience type
#  and only honoured inside `case 'semester':` of studentMatchesAnnouncement, while institute/course/
#  branch were AND-ed before the switch.
#  Fix — BE: dropped the .max(1) caps; extracted studentMatchesAnnouncementScope() so Institute →
#  Course → Branch → Semester are AND-ed for EVERY audience type (empty level = all); branch now
#  falls back to the parent COURSE (students carry no branch attribute — department is course-derived
#  — so strict branch targeting reached nobody; mirrors matchesStudentTargetingForMaster). New
#  GET /announcements/audience/semesters returns the DISTINCT semesters present among students in the
#  selected scope, with per-semester student counts — there is NO course→semester mapping anywhere
#  (AcademicProfile.semester is a bare Int), so deriving from students is what makes cross-selection
#  (B.Tech + a BCA-only semester) impossible. Route registered ABOVE both GET /:announcementId and the
#  router.use(requireRole('student')) line. Reuses the faculty.service.getStudentFilterOptions pattern
#  and the SAME scope predicate as delivery, so picker and recipients cannot disagree.
#  Fix — FE: new AnnouncementAudienceSelector (4 x SearchableMultiSelect; child options are the UNION
#  over selected parents via useQueries on the existing policyKeys.courses/branches cache keys;
#  invalid child selections are pruned, valid ones kept). PolicyAudienceSelector left UNTOUCHED (the
#  policy dialogs still reference it in commented-out code). target_semesters is now always sent;
#  removed the old "Specific Semester" checkbox block, its required-semester guard, and the now-dead
#  SEMESTER_OPTIONS/toggleSelection/Checkbox. "Specific Semester" removed from the audience dropdown
#  (semester is scope, not a mode); legacy 'semester' announcements load as "All Students" with their
#  semesters preserved — identical recipients, since the scope check now ANDs semesters for all types.
#  No schema change, NO MIGRATION (all four target_* columns already exist as String[]).
docs/silveroak_backend/src/modules/announcements/announcement.schema.ts
docs/silveroak_backend/src/modules/announcements/announcement.service.ts
docs/silveroak_backend/src/modules/announcements/announcement.controller.ts
docs/silveroak_backend/src/modules/announcements/announcement.routes.ts
src/components/admin/AnnouncementAudienceSelector.tsx
src/pages/admin/AnnouncementManagement.tsx
src/services/announcementService.ts
src/hooks/use-announcement-api.ts
src/types/announcement.ts

# Enhancement: Announcements — republish an archived announcement
#  An archived announcement had NO actions beyond View: the detail-sheet Publish button rendered only
#  for status === 'draft', and publishAnnouncement threw BusinessRuleError 422
#  INVALID_ANNOUNCEMENT_STATUS ("Only draft announcements can be published") for any non-draft status,
#  so archiving was effectively permanent (including a misclick).
#  Fix — BE: NARROWED (not removed) the guard to reject only an already-published announcement, so
#  draft (first publish) and archived (republish) are both valid inputs. The rest of publishAnnouncement
#  was already republish-ready — it sets published_at = now, CLEARS archived_at, re-syncs metrics and
#  notifies students. Kept the 422 + INVALID_ANNOUNCEMENT_STATUS code so the existing "should reject
#  re-publish" test (published -> publish) keeps passing. Swagger updated; added a test that republishes
#  after the existing archive test.
#  Fix — FE: the detail-sheet action renders for status !== 'published' and is labelled "Republish" for
#  archived; the shared ConfirmActionDialog title/description/confirm label, the success toast and the
#  error fallback are all republish-aware. The dialog explicitly says students are notified again —
#  publish broadcasts to ALL tenant students (pre-existing TODO in the service), so repeated republishing
#  would spam the bell. Per decision: students ARE re-notified, published_at IS overwritten (the original
#  first-publish date is lost), and archived announcements stay non-editable. Read/consent receipts are
#  never deleted, so prior read counts carry over — the read rate does not reset.
#  No schema change, NO MIGRATION.
docs/silveroak_backend/src/modules/announcements/announcement.service.ts
docs/silveroak_backend/src/modules/announcements/announcement.swagger.ts
docs/silveroak_backend/src/__tests__/remaining-modules.test.ts
src/pages/admin/AnnouncementManagement.tsx

# Bugfix: NOC certificate — company name missing from the "To," recipient block
#  The recipient block is HARDCODED (not placeholder-driven, so no template edit could ever fix it)
#  and exists in TWO independent renderers that had drifted: the PDF
#  (noc-certificate.renderer.ts, fixed Y coords 650/635/622 + a rule at 617) and the on-screen
#  NocTemplatePreview. Both emitted only "To," + contact_person_name + contact_person_designation
#  (conditional) — the COMPANY NAME line was never implemented anywhere. Second symptom: the PDF
#  printed "____________________" when contact_person_name was null, which is the normal case for a
#  NOC raised from a TPO-released offer (contact person is mandatory only for self_sourced).
#  Fix: both renderers now emit "To," followed by recipient name, designation and COMPANY NAME, each
#  only when non-empty (underscore fallback removed — per decision, empty lines are skipped, so a
#  no-contact-person NOC reads "To," / "Company Name"). The PDF block is laid out from a cursor
#  (13pt steps from y=650) instead of fixed offsets since the line count is now variable, with the
#  existing rule drawn 5pt below the last line; worst case (3 lines) puts it at y=606, clear of the
#  fixed "Sub:" at y=588. NocTemplatePreview gained a hasValue() guard that also treats the legacy
#  '—' sentinel as empty — buildNocCertificateSnapshot stores that em dash for a missing contact
#  person, so already-issued NOCs would otherwise show a stray dash line.
#  Note: NocTemplatePreview is shared by 4 surfaces (template manager live preview, student
#  certificate page, request detail sheet, TPO review dialog) — one edit covers all of them. Existing
#  issued NOCs re-render from their snapshot so they gain the company line on screen, but their
#  already-written PDF files are NOT regenerated (screen and stored file can differ for historical
#  NOCs; no backfill attempted). No data plumbing needed — company_name was already on the PDF context,
#  in the token map, in the snapshot values and in the preview dummy values.
#  No schema change, NO MIGRATION.
docs/silveroak_backend/src/modules/noc/noc-certificate.renderer.ts
src/components/noc/NocTemplatePreview.tsx

# Bugfix: Offer Records — pagination controls never rendered (+ server-side search)
#  The Previous/Next controls already existed in OffersManagement.tsx but were dead code. The backend
#  sends pagination in snake_case (paginate() -> {page,limit,total,total_pages,has_next,has_prev})
#  while offerService.getOffers returned that payload RAW, cast to PaginatedOffers whose type declares
#  camelCase totalPages. So pagination.totalPages was undefined at runtime and the render guard
#  `(totalPages ?? 1) > 1` was always false — the pager could never appear and admins were stuck on the
#  first 20 records. TypeScript could not catch it (the response is cast through a generic).
#  Fix: added normalizePagination() to offerService (mirrors the 7 services that already do this —
#  adminService/announcement/circular/internship/noDues/notification/policy), accepting BOTH spellings
#  so a future camelCase API still works; PaginatedOffers gained hasNext/hasPrev for parity.
#  Also (per decision) switched the search box from client-side to SERVER-side: the backend already
#  supported ?search (queryOffersSchema + buildOfferWhere, case-insensitive OR over role / company
#  name / student full_name / enrollment_number) so this was FRONTEND-ONLY — no backend change. The
#  local filteredOffers filter was deleted, `search` is passed from the existing useDeferredValue, and
#  the page resets to 1 on a new search (the 3 Selects already did). ⚠ useOffers gained
#  `placeholderData: keepPreviousData` FIRST — search lives in the query key and the page has an early
#  `if (isLoading) return <skeleton/>`, which would otherwise unmount the search input on every
#  keystroke (same trap already fixed in useUsers and usePostings).
#  Behaviour note: server search matches 4 fields; the old client filter also matched posting.title and
#  posting.type, so searching by posting title/type no longer returns matches (widening it is a backend
#  change, out of the agreed scope). CSV export remains current-page-scoped. FacultyOffers.tsx reads the
#  same pagination.totalPages via the same service, so its pager is fixed too with no change to it.
#  No backend change, no schema change, NO MIGRATION.
src/services/offerService.ts
src/types/offer.ts
src/hooks/use-offer-api.ts
src/pages/admin/OffersManagement.tsx

# Tooling: seed-dummy-offers.ts — dummy Offer records for testing the Offer Records list
#  Added to exercise the pagination fix above (needs > 20 rows; the dev DB had 0 offers). Dry-run by
#  default, --apply to insert, --count=N, and --remove --apply to delete again. Every seeded row's
#  `role` is prefixed "[DUMMY]", which is how --remove finds them. ⚠ It only targets students with NO
#  existing offer, because assertNoExistingOffer (shared/utils/offer-block.ts) counts ANY offer row
#  regardless of status — so a seeded student is PERMANENTLY BLOCKED from applying / registering
#  interest until the dummies are removed. Statuses/types/dates are varied so the stat cards, status
#  filter and offer_date sort all have something to show. Script only — no app code, no migration.
docs/silveroak_backend/scripts/seed-dummy-offers.ts

# Bugfix: NOC wizard — Self-Sourced Program/Category listed EVERY posting type in the tenant
#  ⚠ This REVERSES a deliberate 2026-07-06 decision (flagged to and confirmed by the user): the masters
#  `?all_targets=true` flag was added back then specifically so this dropdown would bypass student
#  targeting. NOCRequestWizard called useMasterValues('posting_type', true, true) and the self_sourced
#  branch returned that unfiltered list verbatim, so a B.Tech student saw BCA-only programmes and vice
#  versa. Expected: only posting types the student is enrolled/eligible for or has shown interest in.
#  Fix (FRONTEND-ONLY, no backend change — every endpoint already existed): the self_sourced list is now
#  the union, deduped by normalizeMasterValue, of (a) ELIGIBLE = useMasterValues('posting_type') WITHOUT
#  all_targets, so the server applies matchesStudentTargetingForMaster — an untargeted posting type still
#  matches everyone (matchesTargetValue returns true on an empty target array), which is what keeps the
#  list non-empty in practice; and (b) INTEREST = useStudentInterests() (GET /students/me/interests — a
#  hook that existed but had NO callers) INTERSECTED with the full active catalogue. The all_targets=true
#  query is kept solely for that intersection, which is required because interest_type is a free string
#  holding legacy values (job, summer_internship, OJT - 2026 HONOURS NEP INTERNSHIP, …) that are no longer
#  posting types — offering one would pass the dropdown then fail the wizard's own "valid posting type
#  from masters" validation at submit. The master's spelling wins so the submitted value is canonical.
#  The UNIVERSITY_DRIVE branch is byte-for-byte unchanged (offer-derived types bypass the filter, so a
#  student placed in an out-of-scope type can still raise their NOC). Validation needed no change — it is
#  already fed postingTypeOptions, so it narrows with the dropdown. Empty list blocks the student with
#  "register interest from your Dashboard first" (per decision: no fallback to all types); `program` was
#  already required so no extra guard was added. postingTypeLoading now covers all three queries.
#  No schema change, NO MIGRATION.
src/components/noc/NOCRequestWizard.tsx

# Bugfix: NOC wizard step 2 — Phone and Pincode had NO format validation
#  Both fields were only LENGTH-capped, front and back: validateNocForm checked pincode <= 10 chars and
#  phone <= 20 chars, and createNocSchema declared z.string().max(10)/max(20). So "abcdefgh" was a
#  valid pincode and "hello" a valid phone — values that then reach the issued certificate and the
#  recruiter auto-created on TPO approval.
#  Fix (per decisions: strict Indian format, fields stay OPTIONAL, validate in BOTH layers):
#  - src/lib/nocModule.ts: added isValidIndianMobile / normalizeIndianMobile / isValidIndianPincode
#    beside the existing isValidPan/isValidGst, following the same "optional field — only validate
#    when the user typed something" contract. Mobile = /^[6-9]\d{9}$/ after normalising (strip
#    separators, drop a leading 91 or 0); pincode = /^[1-9]\d{5}$/ (6 digits, no leading zero).
#  - NOCRequestWizard: the two length checks became format checks; the submit payload now sends
#    normalizeIndianMobile(...) so storage is the bare 10 digits however it was typed; placeholders
#    updated ("+91..." -> "10-digit mobile number", "Pincode" -> "6-digit pincode") with inputMode
#    tel/numeric and maxLength=6 on pincode.
#  - noc.schema.ts: indianMobileSchema + indianPincodeSchema mirror the FE rules. ⚠ refine runs BEFORE
#    transform deliberately — normalising first would reduce "hello" to "" and then accept it as an
#    empty optional value. Both keep '' acceptable for non-form callers. The mobile transform also
#    NORMALISES server-side, so a direct API caller's "+91 98765-43210" is stored as "9876543210".
#  Verified by parsing both layers over a case table: 9876543210 / +91 98765-43210 / 098765 43210 /
#  +919876543210 all accepted and all stored as "9876543210"; 1234567890 (starts 1), 12345, hello, abc,
#  98765 4321 all rejected; omitting both fields still parses (they remain optional).
#  ⚠ Deliberately STRICTER than the rest of the app: student/employer/recruiter/signup schemas use a
#  loose /^\+?[\d\s-]{7,20}$/. This rejects landlines and non-Indian numbers on the NOC contact field.
#  Create-path only — these fields appear in no other schema, so no existing NOC row is re-validated
#  and legacy junk values are left as-is. No schema change, NO MIGRATION.
src/lib/nocModule.ts
src/components/noc/NOCRequestWizard.tsx
docs/silveroak_backend/src/modules/noc/noc.schema.ts

# Bugfix: Offer & Joining Management — Posting Type filter 400'd the whole offers list
#  Selecting any Posting Type produced "Unable to load offers — One or more fields have validation
#  errors." and no rows rendered. Cause: the <SelectItem> bound option.VALUE (the human string, e.g.
#  "Test Placement") but that state is sent as `posting_type_master_id`, which queryOffersSchema
#  declares as z.string().uuid(). Zod rejected it → validate() → 400 VALIDATION_ERROR, whose message
#  is literally "One or more fields have validation errors" (error-handler.ts). Because the table is a
#  single query, a rejected filter takes the entire list down rather than just returning nothing.
#  Verified by parsing queryOffersSchema directly: "Test Placement"/"BCA SPECIAL OJT - 3rd Semester"/
#  "job" all REJECTED "Invalid uuid"; a real master UUID parses OK.
#  Fix: bind option.id (the MasterOption UUID) for both `value` and `key` — the documented id-vs-value
#  trap, already fixed the same way in ApplicationsManagement and PostingsManagement. One line; no
#  other state changed (the 'all' sentinel still maps to undefined, the setPage(1) reset stays).
#  ⚠ Do NOT "consistency sweep" this: SelectionDatabaseTab deliberately binds option.value because its
#  endpoint filters by `posting_type` (the value string), not the UUID. Match the param, not a habit.
#  Same defect verified but LEFT UNFIXED per scope decision (identical one-liners): DrivesManagement
#  (vs event.schema.ts posting_type_master_id uuid) and InternshipsManagement (vs internship.schema.ts).
#  Frontend-only; no backend/schema/migration.
src/pages/admin/OffersManagement.tsx

# Bugfix: Events & Drives → Panel Assignment showed NO students
#  EventAssignmentDialog sourced candidates ONLY from applicants to event.posting_id (the LEGACY single
#  mirror = posting_ids[0]), with the query hard-gated on Boolean(event.posting_id), and then filtered
#  OUT everyone already in event.assigned_students. That empties the list in three separate ways:
#  (1) no linked posting (Linked Posting is optional on the Create Event form) -> query disabled and a
#  dead-end "no live candidate source" message; (2) only the FIRST linked role was queried, so
#  applicants to an event's other roles were invisible; (3) nobody applied — the dev DB's only event
#  has a linked posting with 0 applications and 0 assigned students. Excluding assigned students also
#  meant an existing attendee could never be put on a panel or moved between panels — the actual point
#  of "Panel Assignment".
#  Fix (FRONTEND-ONLY — the API already supported all of it): candidates are now one deduped list of
#  (a) event.assigned_students (already in the event detail payload, which carries panel_id + full
#  student details — no extra request), shown with an "Assigned" badge and a new Panel column;
#  (b) applicants across ALL linked roles via useQueries over event.posting_ids (falling back to
#  [posting_id] for pre-migration events), reusing applicationKeys.listAll + getAllApplications (which
#  already stitches every page); and (c) a fallback to the full student directory (useAdminStudents,
#  server-side search, limit 100) when there is no linked role or no applicants — otherwise assignment
#  is impossible. Assigned entries win the dedupe so their current panel shows. Removed the
#  !event.posting_id dead-end branch; empty/loading states now say WHY the list is empty.
#  assignStudents already UPSERTS with `update: { panel_id }`, so re-submitting an assigned student
#  just moves them — no backend change. student_id from the admin list is Student.id (verified in
#  mapAdminStudent), which is what the assign endpoint expects.
#  ⚠ Behaviour notes: "No panel" now CLEARS an assigned student's panel, so it is relabelled
#  "No panel (remove from panel)"; the directory fallback lets ANY student be assigned, not just
#  applicants; and assignment re-notifies the student (pre-existing notifyManyUsers in assignStudents),
#  which is now reachable when merely moving someone between panels.
#  Note: SlotAllocationDialog.tsx (599 lines, panel+slot per student) is DEAD CODE — no imports
#  anywhere and local-state only — do not "fix" panel assignment there.
#  No schema change, NO MIGRATION.
src/components/drives/EventAssignmentDialog.tsx

# Bugfix: Program-specific (posting-type) policy was silently bypassed
#  The gate modal appeared only occasionally and the API accepted applications regardless. Cause: ONE
#  rule applied in TWO places treated a GLOBAL policy acceptance as acceptance of a posting-type
#  policy — `legacyAcceptedAt && legacyAcceptedAt >= policy.updated_at` where legacyAcceptedAt is
#  Student.policy_accepted_at, which student.service.acceptPolicy writes ONLY for global policies
#  (it explicitly skips the update for linked ones). So any student whose global-acceptance timestamp
#  postdated the linked policy's last edit was auto-exempted: isPolicyAcceptedCurrent returned
#  accepted_current: true -> PostingTypePolicyDialog saw nothing pending and proceeded transparently
#  WITHOUT rendering the modal, and assertPostingTypePolicyAccepted never threw. Whether a student was
#  exempt depended on their timestamp vs the policy's updated_at — hence "only occasionally"; editing
#  the policy bumps updated_at and the gate reappears for a while.
#  Verified on the dev DB BEFORE the fix: 3 linked policies (all on "NEW STIPEND BASED INTERNSHIP
#  PROGRAM -2026 5th B.tech") with 0 explicit acceptances between them; Rahul Sharma (global accepted
#  2026-07-21) was auto-exempted from all three. AFTER: calling the real guard for that student now
#  returns BLOCKED -> POSTING_TYPE_POLICY_NOT_ACCEPTED.
#  Fix (BACKEND ONLY — the frontend already engages whenever accepted_current !== true):
#  - policy.service.ts isPolicyAcceptedCurrent: the legacy fallback now applies ONLY when
#    policy.posting_type_master_id === null (global). Threaded that field through
#    attachStudentAcceptanceStatus's generic (types-only; both callers pass rows straight from
#    prisma.policy.findMany). Affects getPolicies + getPolicyById, which can both return linked policies.
#  - shared/utils/posting-type-policy.ts: dropped the fallback entirely — every policy it loads is
#    linked by construction (queried by posting_type_master_id) — so acceptance must be an explicit
#    PolicyAcceptance row matching the policy's current updated_at. Removed the now-unused
#    policy_accepted_at from the student select.
#  ⚠ DO NOT "tidy" the twin helper hasCurrentAcceptance in student.service.ts — it is identical in
#  shape but its only caller pairs it with buildVisiblePolicyWhere, which is hard-clamped to
#  posting_type_master_id: null, so it governs the GLOBAL gate (dashboard access + the documented
#  placement-gate deadlock). Verified unaffected: a globally-accepted student is NOT re-prompted.
#  Behaviour change: students previously exempt are now prompted once. Do NOT backfill acceptances to
#  "preserve" the old state — that would re-create the bug as data.
#  No schema change, NO MIGRATION.
docs/silveroak_backend/src/modules/policies/policy.service.ts
docs/silveroak_backend/src/shared/utils/posting-type-policy.ts

# Enhancement: Employer Management -> Create Company — duplicate company prevention via name auto-suggestion.
# Problem: nothing stopped an admin creating a second Company with an existing name. There is NO unique
#  constraint (schema.prisma has only @@index([tenant_id, name])), createCompany was a bare
#  prisma.company.create, and AddCompanyDialog validated only required/length — so "Infosys", "infosys"
#  and "Info-sys" each became separate companies, fragmenting their postings/recruiters/offers.
#  Note the other two create paths ALREADY deduped by name (importCompanies' knownNames set, and NOC's
#  find-or-create with mode:'insensitive'); manual create was the odd one out.
# Fix (two layers, deliberately different strictness):
#  - Server (authoritative): shared/utils/company-name.ts (NEW) — normalizeCompanyName() lowercases and
#    strips everything that is not a letter/digit (punctuation AND all whitespace, so TechCorp / Tech Corp /
#    Tech-Corp share one key; \p{L}\p{N} not [a-z0-9] so non-Latin names don't normalize to '' and bypass
#    the check). findDuplicateCompany(tenantId, name, excludeCompanyId?) compares IN MEMORY — Prisma's
#    mode:'insensitive' covers case but NOT spacing/punctuation, which is the whole point. Wired into
#    createCompany, and into updateCompany (only when data.name is present, always excluding the edited
#    row so an unchanged name can't match itself). Throws ConflictError 409 COMPANY_ALREADY_EXISTS.
#    Deliberately NOT stripping legal suffixes — that would collide "Acme Ltd" with "Acme Inc".
#  - UI (advisory): AddCompanyDialog debounces the name (300ms, local useEffect — there is no shared
#    useDebounce hook; SearchInput's debounceMs prop is declared but never implemented) and queries the
#    existing GET /companies?search. ⚠ It sends buildCompanySearchTerm() = the LONGEST alphanumeric token,
#    not the raw string: the endpoint matches with a raw `contains`, so "Info-sys" would find nothing and
#    the warning would never appear. Exact match -> banner + Save DISABLED; near matches -> advisory list,
#    Save stays ENABLED (per user decision: "Infosys BPM" must not be blocked by "Infosys"). Each shows a
#    "Use this company" button -> navigate('/admin/companies/:id'); both parents already navigate there for
#    row clicks, so NO new props on CompanyManagement/CompanyListTab. 409 is shown on the name field
#    (not a toast) in BOTH dialogs; EditCompanyDialog gets the 409 handling only, no suggestion UI.
#  - useCompanies(params, enabled = true) gained an optional `enabled` flag so the lookup stays idle until
#    >= 2 chars are typed (same pattern as the student list hooks).
#  ⚠ normalizeCompanyName is MIRRORED front and back (no shared package) — keep the two copies in sync.
#  Pre-existing duplicates are NOT touched (dev DB currently has 5 rows named "Test Corp"); the guard only
#  blocks NEW collisions, and renaming one onto another now 409s. No unique index added on purpose: it
#  would fail to apply where duplicates already exist and still wouldn't catch case/spacing variants.
#  No schema change, NO MIGRATION.
docs/silveroak_backend/src/shared/utils/company-name.ts
docs/silveroak_backend/src/modules/employers/employer.service.ts
src/lib/employerModule.ts
src/hooks/use-employer-api.ts
src/components/employer/AddCompanyDialog.tsx
src/components/employer/EditCompanyDialog.tsx

# Bugfix: TPO Admin -> Postings -> All Postings — the "Type" column showed "Stipend Internship" for every
#  admin-created posting type; header renamed to "Posting Type".
# Problem: getTypeBadge() was a hardcoded 3-branch helper (job -> Placement, internship -> Internship,
#  ELSE -> "Stipend Internship"). Posting types are admin-configurable masters now: the Posting.type enum
#  column was dropped and posting.type is the linked MasterOption's VALUE (flattenPostingType), with
#  ApiPostingType = string. So every custom type hit the catch-all. Verified on the dev DB: of 23 postings,
#  12 are 'job', 3 'internship' and 8 custom ("Placement 2026", "OJT - 2026 HONOURS NEP INTERNSHIP",
#  "Test Placement", "NEW STIPEND BASED INTERNSHIP PROGRAM -2026 5th B.tech", "BCA Posting 1") — and NOT ONE
#  posting is literally 'stipend_internship', so that badge was wrong every time it rendered.
# Fix (FRONTEND ONLY, one file — the correct value was already in the payload):
#  - Deleted getTypeBadge and render the existing shared <PostingTypeBadge> (components/ui/status-badge.tsx),
#    which is already typed status: string and formats via formatPostingTypeLabel (lib/postingModule.ts) —
#    the same helper SelectionDatabaseTab / VerificationTab / InterestPostingsCard / the posting-type filter
#    use. Legacy values keep their pretty labels ('job' -> "Placement (Job)"), anything else is humanized.
#  - Guarded the empty case: flattenPostingType returns '' when no master is linked and
#    formatPostingTypeLabel('') is '' (an empty badge), so a blank type now renders an em dash.
#  - TableHead "Type" -> "Posting Type". Dropped the now-unused getPostingTypeLabel + ApiPostingType imports.
#  ⚠ Do NOT resolve the label via usePostingTypeOptions() — it lists only ACTIVE masters and 6 of the 7
#  in-use types are inactive, so those rows would render blank. posting.type comes from the DB join.
#  No schema change, NO MIGRATION.
#  Left out of scope by user decision (recorded in CLAUDE.md, NOT fixed): getPostingTypeLabel in
#  data/mockPostingsData.ts is a 3-case switch with NO default -> returns undefined, so the type badge is
#  EMPTY on the student OpportunityCard / OpportunityDetail / admin PostingDetail and the opt-out reason
#  reads "opted out of undefined placements"; the "Placements"/"Internships" count cards on this same page
#  still count every non-'job' type as an internship; RecruitmentPipeline.tsx + OfferToJoinFunnelReport.tsx
#  still branch by hand.
src/pages/admin/PostingsManagement.tsx

# Docs: corrected the frontend type-check command in CLAUDE.md.
#  `npx tsc --noEmit` at the repo root checks ZERO files (root tsconfig.json is {"files": [], "references": []}),
#  so it printed nothing and always looked like a pass. The real command is
#  `npx tsc --noEmit -p tsconfig.app.json`, which reports a 100-error baseline — 5 of which were the
#  posting-type-label defect above, flagged by tsc all along in a baseline nobody reads.
CLAUDE.md

# Bugfix: Faculty Coordinator -> My Programs listed the whole course cohort under every program
#  (Reported "records not filtered correctly — common records shown instead of only the relevant program
#  records.") studentUnderProgram (faculty.service.ts) included a student when ENROLLED **or** interested,
#  where enrolled = matchesStudentTargetingForMaster(master.target_*, student) — i.e. the student's
#  course/branch merely falls under the posting type's targeting. For a targeted posting type that is the
#  ENTIRE scoped cohort, so every program rendered the same list as the faculty's Student Directory.
#  Proven on the dev DB (replicated the matcher over 8 faculty / 3 active posting types / 32 students):
#  Mayuresh "NEW STIPEND" showed 23 (= whole scope), only 7 had registered interest; Rakesh 12 (=whole);
#  the BCA programs showed all 6 scoped students with 0 interest. The enrolled ∪ interested rule was
#  DOCUMENTED as intentional in CLAUDE.md, so the new definition was confirmed with the user first
#  (Set Instruction 2).
# Fix (per user decision — membership = explicit signal only: interest OR application OR offer):
#  - facultyProgramStudentInclude now also pulls applications + offers, each selecting
#    posting.posting_type_master_id (apps/offers link to a posting type via that real FK, so match on the
#    master ID — no string normalization; interest stays matched by value string since interest_type is a
#    free column with no FK). loadAssignedPostingTypeMasters select gains `id` (+ PostingTypeMaster type).
#  - studentUnderProgram REWRITTEN: dropped the enrolled / hasTargets / matchesStudentTargetingForMaster
#    path entirely. Returns { interested, applied, registered_at }; applied = any application OR offer whose
#    posting.posting_type_master_id === master.id. Inclusion at both call sites (getAssignedPrograms count +
#    getProgramStudents list) is now interested || applied. Source tag: both | interest | applied.
#  - Removed the now-unused matchesStudentTargetingForMaster import (normalizeComparable stays).
#  - The program MAPPING (facultyMatchesPostingTypeMaster — which programs appear, incl. untargeted
#    "Test Placement" mapping to everyone) is UNCHANGED; only student records changed.
#  - FE: FacultyProgramSource 'enrolled' -> 'applied'; sourceLabel + sourceBadge variant updated.
#  Verified post-fix on the dev DB: Mayuresh "NEW STIPEND" 23 -> 8, Rakesh 12 -> 1, BCA programs -> 0
#  (nobody has engaged yet). No schema change, NO MIGRATION.
docs/silveroak_backend/src/modules/faculty/faculty.service.ts
src/types/faculty.ts
src/pages/faculty/FacultyPrograms.tsx

# Bugfix: Faculty -> Department Students -> View -> Portfolio tab stuck on "Loading..." (never displayed)
#  Two backend bugs on GET /portfolio/:studentId; the FE (PortfolioReadOnlyView + useStudentPortfolio) is fine.
#  (1) portfolio.routes.ts applied a ROUTER-WIDE `router.use(requireRole('student'))` before every route, so a
#      faculty_coordinator hit AuthorizationError ROLE_NOT_ALLOWED (403) and never reached the /:studentId
#      route's OWN requireRole(tpo_admin, tpo_employee, faculty_coordinator, super_admin) — the route-level
#      allowance was dead code. With the default QueryClient (retry=3, backoff) the 403 retried for several
#      seconds = the "stales on loading" spinner. Fix: scope the student-only gate to /me
#      (`router.use('/me', requireRole('student'), requireStudentProfileAccess)`) — every student route lives
#      under /me, and /:studentId (a non-/me path) skips it while keeping its own staff role guard. Dropped the
#      now-unused scopeToDepartment() from the route + its import.
#  (2) Even past the 403, getStudentPortfolio (portfolio.service.ts) scoped faculty by
#      `student.department === scope.department` (scope.department = user.department ?? branches[0]) — the exact,
#      case-sensitive department match the earlier faculty-scope fix already replaced everywhere else. Measured
#      on the dev DB: the exact filter matched 0-1 students where the tolerant Directory shows 6-23, so faculty
#      would 404 on essentially every student. Fix: fetch the student by id+tenant (no department filter; select
#      +course +institute), then for faculty verify studentMatchesFacultyScope(student, resolveFacultyScope(user))
#      — the SAME tolerant matcher as Directory/NOC/My Programs — else NotFound. tpo_admin/tpo_employee/super_admin
#      were already department-filter-free and are unchanged. Dropped the unused `scope` param (controller no
#      longer passes req.scope). Verified with the real shared util: openable set now equals the Directory
#      (8/8/6/11/6/6/12/23), out-of-scope still blocked.
#  Out of scope (flagged, NOT changed): the recruiter Candidate Detail portfolio tab hits the same endpoint but
#  'recruiter' is not in the allowed roles, so it 403s too — a separate access-control decision.
#  No schema change, NO MIGRATION.
docs/silveroak_backend/src/modules/portfolio/portfolio.routes.ts
docs/silveroak_backend/src/modules/portfolio/portfolio.controller.ts
docs/silveroak_backend/src/modules/portfolio/portfolio.service.ts

# Enhancement: TPO Admin -> Master -> Posting Types — per-type "Application Receiving" ON/OFF toggle.
# ⚠ MIGRATION REQUIRED: schema.prisma adds MasterOption.accepting_applications Boolean @default(true)
#   (additive, auto-migratable, no backfill — every existing posting type stays ON). Run:
#   cd docs/silveroak_backend && npx prisma migrate dev
#   (I already ran `npx prisma generate` so the client/TS are current; the DB column is added by the migrate.)
# Behaviour: when a posting type's Application Receiving is OFF, the type stays VISIBLE on the Student
#   Dashboard (NOT hidden — deliberately separate from is_active, which hides it entirely), but the student
#   cannot register interest in it OR apply to its postings (per user decision, apply is blocked even for
#   students who registered interest before it was switched off). All other functionality is unchanged.
# Backend:
#  - schema.prisma: MasterOption.accepting_applications Boolean @default(true).
#  - masters.schema.ts: accepting_applications z.boolean().optional() on create + update schemas; added to the
#    update .refine "at least one field" check.
#  - masters.service.ts: MasterOptionWithTargets type gains the field; mapMasterOption exposes it ONLY for
#    posting_type (like companies); createMaster defaults ?? true; updateMaster passes it through when present.
#    getMasters is NOT filtered by it (type must stay visible).
#  - shared/utils/posting-type-accepting.ts (NEW): assertPostingTypeAcceptingApplications(masterId) throws
#    BusinessRuleError 'POSTING_TYPE_NOT_RECEIVING' (422) when accepting_applications === false. Mirrors the
#    posting-type-policy guard pattern.
#  - application.service.apply(): calls the guard (before assertInterestRegistered) — blocks apply even for
#    already-enrolled students.
#  - student.service.registerInterests(): the masterOptions query now selects accepting_applications and rejects
#    any requested type that is OFF with the same POSTING_TYPE_NOT_RECEIVING error (combined message).
# Frontend:
#  - types/master.ts: ApiMasterOption.accepting_applications?; Create/UpdateMasterInput.accepting_applications?.
#  - use-posting-type-options.ts: PostingTypeOption gains acceptingApplications (master.accepting_applications
#    !== false, default-true for pre-migration rows).
#  - MasterDataManagement.tsx: a Switch labelled "Application Receiving ON/OFF" above the Edit/Deactivate/Delete
#    buttons, posting_type rows ONLY; onCheckedChange -> useUpdateAdminMaster({ accepting_applications }) with a
#    toast, no confirmation dialog (reversible/low-stakes).
#  - Dashboard.tsx: Interest Registration Register button is disabled when the type is OFF (looked up from
#    usePostingTypeOptions via the shared comparison key), with an "Applications closed" badge + helper note;
#    the type stays listed. Registered rows unaffected. Server stays the source of truth (422).
docs/silveroak_backend/prisma/schema.prisma
docs/silveroak_backend/src/modules/masters/masters.schema.ts
docs/silveroak_backend/src/modules/masters/masters.service.ts
docs/silveroak_backend/src/shared/utils/posting-type-accepting.ts
docs/silveroak_backend/src/modules/applications/application.service.ts
docs/silveroak_backend/src/modules/students/student.service.ts
src/types/master.ts
src/hooks/use-posting-type-options.ts
src/pages/admin/MasterDataManagement.tsx
src/pages/Dashboard.tsx

# Bugfix: internal-staff email validation now accepts BOTH @silveroakuni.ac.in and @socet.edu.in.
#  User creation for tpo_admin/tpo_employee/faculty_coordinator/management required an institute email but
#  only allowed the single @silveroakuni.ac.in domain, so staff on @socet.edu.in were rejected — which
#  blocks CRM-fetched staff (their officialEmail is @socet.edu.in, e.g. anita.hr@socet.edu.in in the CRM
#  tests). Both are official domains; the gate now accepts either.
#  Two enforcement points (the only ones — no env/config, no other domain checks; creation-only, login &
#  Edit User are not domain-gated):
#   - BE admin.schema.ts: createUserSchema.superRefine now checks endsWith against an
#     INSTITUTE_EMAIL_DOMAINS = ['@silveroakuni.ac.in','@socet.edu.in'] allowlist (.some); message lists both.
#   - FE UserManagementTab.tsx: INSTITUTE_EMAIL_DOMAIN (single) -> INSTITUTE_EMAIL_DOMAINS array +
#     INSTITUTE_EMAIL_DOMAINS_LABEL; the Add-User pre-submit check, toast, placeholder and helper text updated.
#  ⚠ The allowlist is MIRRORED FE/BE (no shared package) — keep both in sync; the server is authoritative.
#  Verified by parsing the real createUserSchema over a case table: both domains PASS for all four staff
#  roles (case-insensitive), @gmail.com and a "notsocet.edu.in.co" lookalike still EMAIL-FAIL, student/
#  recruiter unaffected. No schema/DB change, NO MIGRATION. seed.ts/tests keep @silveroakuni.ac.in (still valid).
docs/silveroak_backend/src/modules/admin/admin.schema.ts
src/components/superadmin/UserManagementTab.tsx

# Enhancement: stronger password policy — >=8 chars + >=1 uppercase + >=1 special char (was length-only min(8)).
#  Applies wherever a user SETS a password (student signup, forgot/reset, change-password, admin create-user).
#  LOGIN IS DELIBERATELY UNCHANGED (min(1)) so existing weaker passwords still sign in — no forced reset.
#  Special char = any non-alphanumeric (the requirement's @#$%&* are examples; user-confirmed). One shared
#  validator per side rather than editing regexes in a dozen spots.
# Backend (authoritative):
#  - shared/schemas/common.ts: NEW strongPasswordSchema (min8 + max100 + /[A-Z]/ + /[^A-Za-z0-9]/, ordered
#    messages length->uppercase->special).
#  - auth.schema.ts: completeStudentSignupSchema.password, resetPasswordSchema.new_password,
#    changePasswordSchema.new_password -> strongPasswordSchema; their confirm_* fields dropped to
#    z.string().min(1,'Please confirm your password') (the existing equality superRefine reports mismatches,
#    avoids duplicate uppercase/special errors on the confirm field). loginSchema untouched.
#  - admin.schema.ts: createUserSchema.password -> strongPasswordSchema.
#  - shared/utils/password.ts: generateTemporaryPassword now GUARANTEES >=1 uppercase + >=1 special (from a
#    readable @#$%&* set) at two distinct randomBytes-chosen positions, length clamped >=8 — so a generated
#    temp credential itself satisfies the policy. Still crypto-random, still avoids ambiguous 0/O/1/I/l.
# Frontend (mirror for inline UX; server stays source of truth):
#  - src/lib/passwordPolicy.ts: NEW — PASSWORD_POLICY_HINT, getPasswordPolicyError(pw) (first violated rule,
#    same order as BE), strongPasswordFieldSchema (zod). ⚠ mirrored with the BE strongPasswordSchema — keep in sync.
#  - Replaced inline `password.length < 8` checks with getPasswordPolicyError in ChangePasswordDialog,
#    MyProfile, Login (signup + forgot/reset); ADDED the check to Profile change-password and
#    UserManagementTab admin-create (which only checked non-empty). schemas.ts createUserApiSchema.password ->
#    strongPasswordFieldSchema. Added the PASSWORD_POLICY_HINT helper text under each new-password input and
#    replaced the "Minimum 8 characters" placeholder.
#  Verified: parsed the real BE schemas over a case table (short1!/alllower8!/NoSpecial8 rejected with the
#  specific rule; Strong@123 accepted; mismatch -> "Passwords do not match"; login still accepts any non-empty);
#  generateTemporaryPassword sampled 10x all policy-compliant; FE helper matches BE order. No schema/DB change,
#  NO MIGRATION.
docs/silveroak_backend/src/shared/schemas/common.ts
docs/silveroak_backend/src/modules/auth/auth.schema.ts
docs/silveroak_backend/src/modules/admin/admin.schema.ts
docs/silveroak_backend/src/shared/utils/password.ts
src/lib/passwordPolicy.ts
src/lib/schemas.ts
src/components/shared/ChangePasswordDialog.tsx
src/pages/shared/MyProfile.tsx
src/pages/Profile.tsx
src/pages/Login.tsx
src/components/superadmin/UserManagementTab.tsx

# Bugfix: No Dues — enforce one approved record per student (approval-validation was missing).
# Once a student has an approved (or issued) No Dues record, block: a new/resubmitted student
# submission AND any TPO admin review/edit/status action on ANY of that student's requests.
# New guard assertNoApprovedNoDues (approved|issued, throws 422 NO_DUES_ALREADY_APPROVED) is a
# PRE-CHECK in createNoDues, resubmitNoDues, updateNoDues and reviewNoDues (the single endpoint
# behind Approve/Reject/Return/Change-Status) so the FIRST approval still succeeds; issueNoDues is
# NOT guarded (legitimate approved->issued completion). FE: hide Edit/Change-Status actions on an
# approved request (admin) so the "Change Status" loophole is closed. No schema change, no migration.
docs/silveroak_backend/src/shared/utils/no-dues-block.ts
docs/silveroak_backend/src/modules/no-dues/no-dues.service.ts
src/pages/admin/NoDuesManagement.tsx

# Bugfix: No Dues approval closes the student's placement & internship process.
# Once a student has an approved (or issued) No Dues record, block Apply, Show Interest (register
# interest), and requesting a new NOC. New guards hasApprovedNoDues + assertPlacementOpenForNoDues
# (throws 422 NO_DUES_PROCESS_CLOSED) added to no-dues-block.ts and wired as early pre-checks into
# application.apply(), student.registerInterests(), and noc.createNoc(). getMyProfile now returns a
# no_dues_closed boolean; FE disables the Apply / Register / Request-New-NOC buttons + shows a banner
# (records stay viewable). Offer accept/reject and view-only pages intentionally left open. No migration.
docs/silveroak_backend/src/shared/utils/no-dues-block.ts
docs/silveroak_backend/src/modules/applications/application.service.ts
docs/silveroak_backend/src/modules/students/student.service.ts
docs/silveroak_backend/src/modules/noc/noc.service.ts
src/types/student.ts
src/pages/OpportunityDetail.tsx
src/pages/Dashboard.tsx
src/pages/NOCDashboard.tsx
src/lib/noDuesModule.ts

# Enhancement: TPO approval required before a student joins a Program/Posting Type + TPO withdraw.
# ⚠ MIGRATION REQUIRED: schema.prisma adds `enum InterestRegistrationStatus {pending approved withdrawn}`
#   and InterestRegistration.status (@default(approved) → grandfathers existing rows), reviewed_by,
#   reviewed_at, status_reason, and @@index([student_id, status]). Run: npx prisma migrate dev.
# ⚠ ONE-TIME BACKFILL (existing tenants): tpo_admin needs can_approve on interest_lists (RolePermission
#   rows are already seeded, so the new default only applies to new tenants). Run from docs/silveroak_backend:
#   tsx scripts/backfill-interest-lists-approve.ts --apply
# Registering interest now writes status=pending; a pending registration keeps posting VISIBILITY but
# the Apply gate requires status=approved (assertInterestRegistered → 422 POSTING_TYPE_PENDING_APPROVAL).
# New TPO endpoints: PUT /admin/interests/registrations/:id/approve and /withdraw (interest_lists:approve).
# Withdraw is soft (status=withdrawn, keeps history); withdrawn rows excluded from lists/summary/faculty/
# notifications/reports; student may re-register (→ pending). FE: student badges (Registered/Pending),
# OpportunityDetail apply reason, admin InterestLists status filter + Approve/Withdraw actions.
# ⚠ AMENDED 2026-08-06 (see the "Interest withdrawal — audit + student message + lock" block below):
#   (a) "student may re-register (→ pending)" is REVERSED — a withdrawn row is now TERMINAL for the student
#       (registerInterests throws INTEREST_WITHDRAWN); only a TPO admin reinstates via approve.
#   (b) "withdrawn rows excluded from lists" now applies to summary/faculty/notifications/reports ONLY —
#       the admin Interest List (getInterestRegistrations) DEFAULT now SHOWS withdrawn rows.
docs/silveroak_backend/prisma/schema.prisma
docs/silveroak_backend/src/modules/students/student.service.ts
docs/silveroak_backend/src/shared/utils/posting-type-interest.ts
docs/silveroak_backend/src/shared/permissions.ts
docs/silveroak_backend/src/modules/admin/admin.service.ts
docs/silveroak_backend/src/modules/admin/admin.controller.ts
docs/silveroak_backend/src/modules/admin/admin.routes.ts
docs/silveroak_backend/src/modules/admin/admin.schema.ts
docs/silveroak_backend/src/modules/faculty/faculty.service.ts
docs/silveroak_backend/src/modules/notifications/notification.service.ts
docs/silveroak_backend/src/modules/reports/report.analytics.service.ts
docs/silveroak_backend/scripts/backfill-interest-lists-approve.ts
src/types/student.ts
src/types/admin.ts
src/pages/Dashboard.tsx
src/pages/OpportunityDetail.tsx
src/pages/admin/InterestLists.tsx
src/services/adminService.ts
src/hooks/use-admin-api.ts

# Bugfix: Forms now scroll to + focus the first validation error on submit.
# New shared helper src/lib/formErrors.ts (scrollToFirstError + focusFirstFormError onInvalid handler)
# keys off the aria-invalid="true" attribute that shadcn FormControl already sets on every errored
# control (native inputs AND Select triggers). Wired as the 2nd arg of handleSubmit in the ~14
# react-hook-form forms. NOC wizard (manual state): FieldError now renders a data-field-error anchor;
# after the existing step-navigation to the first error, focusFirstStepError() scrolls+focuses the
# field. Frontend-only, no backend, no migration.
src/lib/formErrors.ts
src/pages/NoDuesCertificate.tsx
src/pages/Login.tsx
src/components/admin/AddPolicyDialog.tsx
src/components/admin/EditPolicyDialog.tsx
src/components/offers/CreateOfferDialog.tsx
src/components/offers/RejectOfferDialog.tsx
src/components/offers/JoiningConfirmationDialog.tsx
src/components/employer/EditEngagementDialog.tsx
src/components/internships/CreateInternshipDialog.tsx
src/components/internships/AddInternshipDialog.tsx
src/components/profile/AddCertificationDialog.tsx
src/components/profile/EditCertificationDialog.tsx
src/components/portfolio/PortfolioProjectDialog.tsx
src/components/portfolio/PortfolioShowcaseDialog.tsx
src/components/noc/NOCRequestWizard.tsx

# Enhancement: Standardize every posting-type FILTER dropdown label to "Posting Type" / "All Posting Types".
# OpportunityFilters was still "Type"/"All Types"; seven others read lowercase "Posting type" — all now
# "Posting Type". Also fixed two latent id-vs-value binding bugs: DrivesManagement + InternshipsManagement
# posting-type filters bound option.value but send posting_type_master_id (uuid) → 400; now bind option.id.
# SelectionDatabase label-only (its param is the value-string enum posting_type, binding stays option.value).
# Non-posting "Type" filters (event type, NOC type, internship payment type) intentionally untouched.
# Frontend-only, no backend, no migration.
src/components/opportunities/OpportunityFilters.tsx
src/pages/admin/OffersManagement.tsx
src/pages/admin/ApplicationsManagement.tsx
src/pages/admin/DrivesManagement.tsx
src/pages/admin/InternshipsManagement.tsx
src/components/admin/StudentListTab.tsx
src/components/admin/VerificationTab.tsx
src/components/admin/SelectionDatabaseTab.tsx

# Enhancement: NOC wizard (student, self-sourced) — "Other – Enter New Company" pinned to TOP of the
# company search and always visible even while typing. SearchableSelect gained an optional pinnedOptions
# prop (rendered in a top CommandGroup; each pinned item appends the live search term to its cmdk value so
# it never gets filtered out). The NOC company field moves its Other entry from an appended option into
# pinnedOptions. Fixes the case where searching for a not-listed company hid the Other escape. FE-only,
# no backend, no migration.
src/components/shared/SearchableSelect.tsx
src/components/noc/NOCRequestWizard.tsx

# Enhancement: NOC Management summary — add a "Pending Faculty" counter card.
# Mirrors the existing per-status useNocs({limit:1,status}) count pattern; grid bumped 5→6 columns.
# FE-only, no backend, no migration.
src/pages/admin/AdminNOCManagement.tsx

# Enhancement: NOC apply form (student) — add a required "Internship Type" radio (Internship / Placement)
# after the Start/End Date fields on step 3, persisted on the NOC request and shown on the NOC detail
# + admin review dialog.
# ⚠ MIGRATION REQUIRED: schema.prisma NocRequest.internship_type String? @db.VarChar(20) (nullable,
#   additive). Run: cd docs/silveroak_backend && npx prisma migrate dev
# Create schema accepts internship_type optionally (FE enforces required so existing API tests/seed
# don't break); service writes it; list/detail return the raw row so it flows to the FE automatically.
docs/silveroak_backend/prisma/schema.prisma
docs/silveroak_backend/src/modules/noc/noc.schema.ts
docs/silveroak_backend/src/modules/noc/noc.service.ts
src/components/noc/NOCRequestWizard.tsx
src/components/noc/NOCRequestDetailSheet.tsx
src/components/noc/NOCReviewDialog.tsx
src/types/noc.ts

# Enhancement: TPO -> Reports — apply "EXISTING REPORTS REMARK" meeting sheet, PASS 1 (mechanical, FE-only).
# (1) Unregistered 25 reports from the reportModules registry (render guards/components left as dead code);
#     4 emptied categories (Employer, Internships, Portfolio, Communication) dropped; default activeReport
#     moved off the removed 'interested' to 'eligibility'.
# (2) Removed Print & PDF buttons globally in the shared ReportToolbar (kept CSV) + fixed the AuditLogTab
#     consumer that passed onExportPDF/onPrint.
# (3) CSV "Roll Number" -> "Enrollment Number" (header + value): Eligibility & Profile Completion (inline
#     handleExport) + Student Participation (label-only; its value was already the enrollment number).
# (4) Dropped the "All Department" dropdown from Shortlist vs Rejection and Offer Acceptance Summary.
# (5) Posting History by Year: removed the per-year summary "boxes" (kept the year filter dropdown).
# PASS 2 (deferred): posting-type regroup (Registration Summary), NOC-by-Dept posting-type filter,
# Posting-History posting-type breakdown fix, institute/course/branch/semester for Student Participation +
# Unplaced Students, Eligibility redesign. FE-only, no backend, no migration.
src/pages/admin/ReportsAnalytics.tsx
src/components/reports/ReportToolbar.tsx
src/components/reports/ShortlistRejectionReport.tsx
src/components/reports/OfferAcceptanceSummary.tsx
src/components/reports/StudentParticipationHistory.tsx
src/components/superadmin/AuditLogTab.tsx

# Enhancement: TPO -> Reports — apply "EXISTING REPORTS REMARK" meeting sheet, PASS 2 (backend-coupled + redesign).
# (7) Registration Summary — regroup by Posting Type: backend groups InterestRegistration dynamically by the
#     distinct interest_type value (= posting-type master value; includes legacy/admin values) instead of the
#     4 hardcoded INTEREST_REPORT_TYPES buckets (constant removed); returns a humanized `label` per row. FE
#     table/CSV relabeled "Program Type" -> "Posting Type" and prefer `reg.label`.
# (8) NOC by Department/Batch — replaced the NOC-Type dropdown with a Posting Type dropdown
#     (usePostingTypeOptions, binds option.value); backend filters NocRequest by `program` (normalized equals)
#     instead of noc_type.
# (9) Posting History by Year — added a "Posting Type Summary" card (FE-only) computing total/draft/published/
#     closed per actual posting.type from the filtered rows (respects year+type filters); replaces the missing
#     posting-type-wise breakdown.
# (10) Student Participation History — removed the lone Branch dropdown; added Institute/Course/Branch filters
#     + columns (+CSV). Backend select now pulls student institute/course; where filters institute/course/
#     branch(=department); mapper returns institute/course.
# (11) Unplaced Students — removed the Department filter/column; added Institute/Course/Branch/Semester filters
#     + columns (+CSV). Backend where filters institute/course/branch(=department); semester filtered in-memory
#     (current_semester ?? academic_profile.semester); mapper appends institute/course/semester;
#     dept_breakdown also returned as branch_breakdown (badges now set the Branch filter).
# (12) Eligibility Report — dropped the student-wise table; added a Department-wise Eligibility breakdown
#     (per-dept eligible% Progress bar + eligible/conditional/not-eligible chips), mirroring Profile Completion.
# Branch == the course-derived `department` column throughout (students carry no separate branch attribute).
# NO schema change, NO migration. Backend restart picks up the reports service change.
docs/silveroak_backend/src/modules/reports/report.analytics.service.ts
src/pages/admin/ReportsAnalytics.tsx
src/components/reports/NOCByDepartmentReport.tsx
src/components/reports/StudentParticipationHistory.tsx
src/components/reports/UnplacedStudentsReport.tsx

# Enhancement: TPO -> Reports — build the 8 NEW REPORTS from the "NEW REPORTS" meeting sheet.
# New "New Reports" module in the reportModules registry with 8 delegated reports across 4 domains:
#  (1) Placement Count, (2) Placement Data Listing/Export, (3) Internship/NOC Count,
#  (4) Internship/NOC Listing/Export, (5) No-Due Count, (6) No-Due Listing/Export,
#  (7) Company Data Count, (8) Company Stage-wise.
# Shared filter bar = ReportScopeFilters (multi-select Posting Type by master VALUE + Institute/Course/
#  Branch/Semester single-selects + Academic Year); shared export via ReportExportHeader (downloadCsvTable/
#  downloadExcelTable). "Branch" == the course-derived Student.department everywhere.
# Backend: 8 new functions in report.analytics.service.ts (reusing average/median/minPositive/parseMoneyValue/
#  groupBy/matchesSelectedValues/studentMatchesSelection/matchesStudentTargetingForMaster), + median/minPositive
#  helpers, wired through report.controller.ts + report.routes.ts (/reports/placement-count, /placement-listing,
#  /internship-noc-count, /internship-noc-listing, /no-dues-count, /no-dues-listing, /company-count, /company-stage).
#  Count reports group by posting-type master value x institute/course/branch/semester; eligible = AcademicProfile.cgpa>=6.5.
# Read-only data-gap handling (no schema for these): NOC completion cert/link = 0/—, placement offer-letter from a
#  matching NOC else —, internship+placement status derived (accepted offer OR issued NOC), No-Due Passing Year = batch.
# ⚠ MIGRATION REQUIRED: schema.prisma enum ExitReason += planning_studies, competitive_exam (No-Due 5-way breakdown).
#  Existing 3 values kept (no backfill). Run: cd docs/silveroak_backend && npx prisma migrate dev
#  Mirrored FE/BE: no-dues.schema.ts, src/lib/schemas.ts exitReasonEnum, src/types/noDues.ts (type + EXIT_REASON_LABELS),
#  NoDuesCertificate.tsx student form (2 new discriminated-union branches + options + payload/fromRequest),
#  noDuesModule.getNoDuesSummary fallback.
docs/silveroak_backend/prisma/schema.prisma
docs/silveroak_backend/src/modules/reports/report.analytics.service.ts
docs/silveroak_backend/src/modules/reports/report.controller.ts
docs/silveroak_backend/src/modules/reports/report.routes.ts
docs/silveroak_backend/src/modules/no-dues/no-dues.schema.ts
src/services/reportService.ts
src/hooks/use-report-analytics-api.ts
src/components/reports/ReportScopeFilters.tsx
src/components/reports/ReportExportHeader.tsx
src/components/reports/PlacementCountReport.tsx
src/components/reports/PlacementListingReport.tsx
src/components/reports/InternshipNocCountReport.tsx
src/components/reports/InternshipNocListingReport.tsx
src/components/reports/NoDuesCountReport.tsx
src/components/reports/NoDuesListingReport.tsx
src/components/reports/CompanyCountReport.tsx
src/components/reports/CompanyStageReport.tsx
src/pages/admin/ReportsAnalytics.tsx
src/pages/NoDuesCertificate.tsx
src/types/noDues.ts
src/lib/schemas.ts
src/lib/noDuesModule.ts

# Enhancement (follow-up): distribute the 8 New Reports under existing sidebar branches instead of a standalone
# "New Reports" module. Placement Count/Listing -> Placement Analytics; Internship/NOC Count/Listing -> NOC &
# Documents; Company Count/Stage -> Applications & ATS; No-Due Count/Listing -> new "No Dues" module (id:'no-dues').
# Render guards re-keyed to placement-analytics/noc/ats/no-dues. FE-only registry change, no backend/migration.
src/pages/admin/ReportsAnalytics.tsx

# Enhancement: FILTER COUNTER EXPORT sheet — TPO Admin, Phase 0 (shared infra) + Phase 1 (Student Management).
# Phase 0: shared reusable pieces.
#  - FE AdminListScopeFilters (config-driven filter bar: cascading Institute->Course->Branch via policy hooks,
#    static Semester 1-8, Academic Year / Passing Year via useMasterValues, Posting Type id-bound, date-range popover).
#  - BE shared/utils/student-scope-filter.ts (buildStudentScopeConditions / buildStudentScopeRelation /
#    buildDateRangeCondition) — institute/course/branch(=department)/semester(=current_semester)/academic_year(=batch)
#    case-insensitive contains, reusable directly on Student lists or nested as {student:...} for related lists.
# Phase 1 (Student Management):
#  - All Students (StudentListTab): added Institute/Course/Branch/Semester + Company + date-range filters; replaced the
#    hand-rolled CSV with a CSV + Excel export over ALL matching rows (loops adminService.getStudents) using the sheet's
#    full column set (name, mobile, gender, email, enrollment, institute, course, branch, semester, CPI, 12th, 10th,
#    backlog, academic year, project count, internship count, status, resume link). BE getStudents + queryStudentsSchema
#    + buildStudentWhere extended (institute/course/branch/semester/academic_year/company_id/date_from/date_to).
#  - Verification (VerificationTab): added Academic Year filter (backend already accepts academic_year->batch).
#  - Portfolio (PortfolioMonitoringTab): added Institute/Course/Branch/Semester filters; REMOVED the Export button.
#    BE getPortfolios + queryPortfoliosSchema extended.
#  - Selection Database (SelectionDatabaseTab): added Institute/Course/Branch/Academic-Year filters; export column set
#    expanded per sheet (name, mobile, gender, email, enrollment, institute, course, branch, semester, CPI, 12th, 10th,
#    backlog, academic year, status, joining date, company, role, selection date, NOC status) via downloadCsvTable.
#    BE getSelectionDatabase: richer student select + selectionStudentFields + applySelectionFilters extended + schema.
#  - DEFERRED: Eligibility Rules institute/course dropdowns (pending scope-vs-persist decision — would need rule columns).
#  - Already satisfied (skipped): Selection Database PDF/Print (removed in Reports pass); no top-level Placement/Internship tab.
# FE-only + BE filter params. NO schema change, NO migration. FE tsc 94 (baseline), BE tsc 0.
docs/silveroak_backend/src/shared/utils/student-scope-filter.ts
docs/silveroak_backend/src/modules/admin/admin.service.ts
docs/silveroak_backend/src/modules/admin/admin.schema.ts
src/components/admin/AdminListScopeFilters.tsx
src/components/admin/StudentListTab.tsx
src/components/admin/VerificationTab.tsx
src/components/admin/PortfolioMonitoringTab.tsx
src/components/admin/SelectionDatabaseTab.tsx
src/types/admin.ts

# Enhancement: FILTER COUNTER EXPORT sheet — TPO Admin Phase 2 (Employer / Posting / Applications / Offers)
# Reuses the Phase 0 shared pieces (AdminListScopeFilters FE + student-scope-filter.ts BE). Per user decision at
# the Phase-2 checkpoint: Postings get ICB (target-array based) + Academic-Year + Date + Export; Applications get
# Posting Type + a SEPARATE Academic-Year filter side by side (not one merged dropdown).
#  - Offers (OffersManagement): Institute/Course/Branch/Semester/Academic-Year + date-range (offer_date) filters via
#    AdminListScopeFilters; moved Export + Create Offer buttons out of the filter flex row into the card header and gave
#    search a min-width (search-box sizing fix); added a "Rejected by student" counter card (grid 4->5).
#    BE offer.service buildOfferWhere: student-scope conditions merged into where.student + offer_date date range;
#    offer.schema queryOffersSchema extended.
#  - Applications (ApplicationsManagement): Institute/Course/Branch/Semester/Academic-Year + date-range (applied_at)
#    filters; added an "Applied" counter (grid 6->7); Resume Link now in the export field set. BE application.service
#    getApplicationsByPosting: student-scope + applied_at date range + resume include exposing resume_url on the item;
#    application.schema extended. applicationExport.ts: new "Resume Link" field (resolveBackendAssetUrl).
#  - Application Pipeline (ApplicationPipeline): swapped the client-side page-derived Branch dropdown for server-side
#    Institute/Course/Branch (AdminListScopeFilters) + date-range on the posting's application query.
#  - Posting Operations (PostingsManagement): Institute/Course/Branch (target arrays) + Academic-Year + date-range
#    (created_at) filters; added CSV + Excel Export over ALL matching postings (Company, Title, Role, Posting Type,
#    Academic Year, Bond, Min CGPA, Max Backlogs, App Start/End, Package/Stipend, Status, Created). BE posting.service
#    getPostings: target-array has() + academic_year + created_at range (admin path); posting.schema extended.
#  - Companies (CompanyListTab): Sector/Industry dropdown (distinct industries from a limit-100 fetch) + date-range
#    (created_at) filters; added an "Inactive" counter (grid 4->5); added recruiter-aware CSV + Excel Export (one row
#    per recruiter, companies with none get a blank-recruiter row) joining employerService.getCompanies +
#    getRecruiters. BE employer.service getCompanies: industry contains + created_at range; employer.schema extended.
#  - Recruiters (RecruiterListTab): added a "Rejected" counter (grid 3->4) + CSV + Excel Export (name/email/contact/
#    designation/company/status/added-on) looping employerService.getRecruiters.
# FE types: OfferQueryParams / ApplicationQueryParams / PostingQueryParams / CompanyQueryParams extended with the new
# keys; ApiApplicationListItem gained resume_url; ApiPostingListItem gained academic_year + bond_details (already in
# the list payload). NO schema change, NO migration. FE tsc 92 (<=94 baseline), BE tsc 0.
# DEFERRED (Phase 1): Eligibility Rules institute/course dropdowns (scope-vs-persist decision).
docs/silveroak_backend/src/modules/offers/offer.schema.ts
docs/silveroak_backend/src/modules/offers/offer.service.ts
docs/silveroak_backend/src/modules/applications/application.schema.ts
docs/silveroak_backend/src/modules/applications/application.service.ts
docs/silveroak_backend/src/modules/postings/posting.schema.ts
docs/silveroak_backend/src/modules/postings/posting.service.ts
docs/silveroak_backend/src/modules/employers/employer.schema.ts
docs/silveroak_backend/src/modules/employers/employer.service.ts
src/types/offer.ts
src/types/application.ts
src/types/posting.ts
src/types/employer.ts
src/lib/applicationExport.ts
src/pages/admin/OffersManagement.tsx
src/pages/admin/ApplicationsManagement.tsx
src/pages/admin/ApplicationPipeline.tsx
src/pages/admin/PostingsManagement.tsx
src/components/admin/CompanyListTab.tsx
src/components/admin/RecruiterListTab.tsx

# Enhancement: FILTER COUNTER EXPORT sheet — TPO Admin Phase 3 (NOC / Events / Announcements / No-Dues / Policy / Interest)
# Reuses the Phase 0 shared pieces (AdminListScopeFilters FE + student-scope-filter.ts BE). Per user decisions at the
# Phase-3 checkpoint: NOC "Ready to Issue" tab REMOVED (approved NOCs are issued from All Requests via the row Review
# action); "Completion Certificate" tab SKIPPED (no backing data); event attendance import/export SKIPPED.
#  - NOC (AdminNOCManagement): added Posting-Type (bound option.VALUE → NOC.program) + Institute/Course/Branch +
#    Academic-Year + date-range filters. Tabs reworked: removed "Ready to Issue"; added "Pending by Faculty" +
#    "Rejected"; kept Pending TPO / Issued / All Requests (default tab now Pending by Faculty). Issuance path: approved
#    NOCs surface in All Requests (Status=Approved) and issue via Review (unchanged NOCReviewDialog). BE noc.service
#    getNocs where: program equals posting_type + student-scope (ICB/academic_year) + created_at range; noc.schema +
#    FE NocQueryParams extended.
#  - Events & Drives (DrivesManagement): added "Ongoing" + "Cancelled" counter cards (grid 4→6); Institute/Course/
#    Branch (pipeline target arrays) + event-date range filters; renamed the "Cancel" status action → "Event Cancel";
#    added CSV + Excel event Export (Event Name, Company, Date, Start/End, Venue, Type, Status, Faculty Coordinators,
#    Panels, Registered, Linked Roles) over all matching events. Attendance import/export deliberately NOT built.
#    BE event.service getEvents where: target-array ICB + date range; event.schema + FE EventQueryParams extended.
#  - Announcements (AnnouncementManagement): Institute/Course/Branch (target arrays) + created-date range filters.
#    BE announcement.service admin getAnnouncements where + announcement.schema + FE AnnouncementQueryParams extended.
#  - No Dues (NoDuesManagement): added "Returned" + "Rejected" counter cards (grid 5→7); Institute/Course/Branch +
#    Passing-Year (batch) filters (also threaded through Export All). BE no-dues.service getNoDuesRequests where:
#    student-scope relation + created_at range; no-dues.schema + FE NoDuesQueryParams extended.
#  - Policy Repository (PolicyRepository): REMOVED the category badge-pill filters (+ categoryFilter state,
#    policyCategoryOptions memo, policyCategoryQuery/useMasterValues). FE-only.
#  - Interest Lists (InterestLists): Institute/Course/Branch + Semester + Academic-Year + registration-date range
#    filters (the date range filters the matching registration's created_at). BE admin.service getInterestRegistrations
#    conditions: student-scope + registration-some created_at; admin.schema queryInterestRegistrationsSchema + FE
#    InterestRegistrationsQueryParams extended.
# FE types: NocQueryParams / EventQueryParams / AnnouncementQueryParams / NoDuesQueryParams / InterestRegistrationsQueryParams
# extended with the new keys. NO schema change, NO migration. FE tsc 91 (<=94 baseline), BE tsc 0.
docs/silveroak_backend/src/modules/noc/noc.schema.ts
docs/silveroak_backend/src/modules/noc/noc.service.ts
docs/silveroak_backend/src/modules/events/event.schema.ts
docs/silveroak_backend/src/modules/events/event.service.ts
docs/silveroak_backend/src/modules/announcements/announcement.schema.ts
docs/silveroak_backend/src/modules/announcements/announcement.service.ts
docs/silveroak_backend/src/modules/no-dues/no-dues.schema.ts
docs/silveroak_backend/src/modules/no-dues/no-dues.service.ts
docs/silveroak_backend/src/modules/admin/admin.schema.ts
docs/silveroak_backend/src/modules/admin/admin.service.ts
src/types/noc.ts
src/types/event.ts
src/types/announcement.ts
src/types/noDues.ts
src/types/admin.ts
src/pages/admin/AdminNOCManagement.tsx
src/pages/admin/DrivesManagement.tsx
src/pages/admin/AnnouncementManagement.tsx
src/pages/admin/NoDuesManagement.tsx
src/pages/admin/PolicyRepository.tsx
src/pages/admin/InterestLists.tsx

# Enhancement: FILTER COUNTER EXPORT sheet — Faculty Coordinator role (Dashboard / Department Students / My Programs / NOC Approvals / Offers / Department Events)
# Second role of the sheet (rows 85–115); reuses the TPO-Admin shared pieces (AdminListScopeFilters FE +
# student-scope-filter.ts BE). Faculty NOC + Offers pages already use the shared useNocs/useOffers endpoints whose
# filters were built in the TPO phases, so those needed no new backend. NO schema change, NO migration.
#  - Faculty Dashboard (FacultyDashboard): added "Total Offer Released" + "Join" counters and renamed "Placed Students"
#    → "Accepted". BE faculty.service.getDashboard tallies offersReleased/accepted/joined over the scoped students'
#    already-loaded offers; FacultyDashboardStats (BE + FE type) gained the 3 fields. Grid 4→6.
#  - Department Students (DepartmentStudents): replaced the Institute/Branch/Semester multi-selects with the shared
#    ERP cascade AdminListScopeFilters (Institute→Course→Branch + Semester + date range); relabeled the "Roll Number"
#    column + CSV header → "Enrollment No" and switched the value to enrollment_number; added an Excel export
#    (CSV+Excel via downloadCsvTable/downloadExcelTable). BE faculty.schema queryFacultyStudentsSchema gained course +
#    date_from/date_to; buildStudentWhere applies course (ci contains) + created_at range; FE FacultyStudentsQueryParams
#    widened (single-or-array institute/branch/semester + course + dates).
#  - My Programs (FacultyPrograms): export renamed "Registered On" → "Interested On Date" and added a Gender column;
#    the data-listing "Source" column header → "Status". BE faculty.service.mapProgramStudent returns gender;
#    FacultyProgramStudent type gained gender.
#  - Faculty NOC Approvals (FacultyNOCApprovals): added Posting-Type (value-bound) + Academic-Year + date-range filters
#    (table via a filtered useNocs; counters from an unfiltered dept fetch); added "Approved by TPO" + "Rejected by
#    Department" + "Rejected by TPO" counters (derived from status + faculty_approved_at, since ApiNocRecord has no
#    decision flag); added a "NOC Number" table column (shown when issued); added a CSV+Excel Export (Student, enrollment,
#    Institute, Course, Branch, Semester, Posting type, Company, Technology/Domain, Stipend, Status, Offer-Letter link,
#    NOC Cert Number, NOC Cert link, Completion-Cert link=blank read-only gap). No BE change.
#  - Faculty Offers (FacultyOffers): removed "Joined" + "Blocked" counters, added "Pending" + "Rejected"; added
#    Academic-Year + date-range + Posting-Type (id-bound) filters; added "Rejected by Students" to the status dropdown;
#    added a CSV+Excel Export (Student, Enrollment, Institute, Course, Branch, Semester, Posting type, Company, Role,
#    Stipend, Acceptance Status, Joining status). BE offer.service.getOffers student select gained institute/course/
#    current_semester (additive) so the export columns aren't blank; ApiOfferListItem.student widened.
#  - Department Events (FacultyDrives): added a client-side Search box (title/company/venue/type). FE-only.
# FE tsc 91 (baseline), BE tsc 0.
docs/silveroak_backend/src/modules/faculty/faculty.service.ts
docs/silveroak_backend/src/modules/faculty/faculty.schema.ts
docs/silveroak_backend/src/modules/offers/offer.service.ts
src/types/faculty.ts
src/types/offer.ts
src/pages/faculty/FacultyDashboard.tsx
src/pages/faculty/DepartmentStudents.tsx
src/pages/faculty/FacultyPrograms.tsx
src/pages/faculty/FacultyNOCApprovals.tsx
src/pages/faculty/FacultyOffers.tsx
src/pages/faculty/FacultyDrives.tsx

# Enhancement: FILTER COUNTER EXPORT sheet — STUDENT role (sheet rows r117–126). Frontend-only,
# all client-side (student endpoints /applications/my, /noc/my, /events/my, /announcements fetch
# the full list; filtering is in-memory). No BE change, no schema, no migration.
#  - My Applications (MyApplications.tsx): REMOVED the "Internships" tab (TabsTrigger + TabsContent;
#    dropped the now-unused StudentInternshipsTab + useStudentProfile imports and profileQuery — Loader2
#    kept, still used elsewhere). ADDED a "Posting Type" single-select filter beside the search box on the
#    Applications tab (usePostingTypeOptions, SelectItem value=option.value since ApiMyApplication.posting.type
#    is the master VALUE string; predicate extended in filteredApplications).
#  - NOC Requests (NOCDashboard.tsx): ADDED a "Posting Type" single-select filter (value-bound) in the row
#    with "Request New NOC"; pre-filters requests by request.program before the active/completed split, so the
#    stat cards + tabs all narrow together.
#  - My Events & Drives (StudentDrives.tsx): ADDED a client-side Search box (title/company/venue/type),
#    direct port of FacultyDrives (useDeferredValue + filteredEvents useMemo; upcoming/past derive from it).
#  - Announcements (StudentAnnouncements.tsx): ADDED a client-side Search box (title/content/audience/author);
#    hooks declared above the early loading/error returns (Rules of Hooks); empty-state distinguishes
#    "no announcements" vs "no matching".
#  - Super Admin Audit Logs (sheet r128–130): SKIPPED per user — role-wise filter already exists, and
#    "Print & PDF" was already globally removed from ReportToolbar (Pass 1, 2026-08-04). No change.
# FE tsc 91 (baseline, zero net-new), BE untouched.
src/pages/MyApplications.tsx
src/pages/NOCDashboard.tsx
src/pages/StudentDrives.tsx
src/pages/StudentAnnouncements.tsx

# Enhancement: FILTER COUNTER EXPORT sheet — SUPER ADMIN Audit Logs role filter (sheet r130).
# Added a "Role" filter to Super Admin → Security & Access Control → Audit Logs (previously only
# Action / Module / specific-user). SERVER-SIDE (correct across pagination): ApiAuditLog.user has no
# role, so client-side filtering was impossible — instead getAuditLogs filters the actor via the
# already-included user relation.
#  - BE admin.schema.ts: queryAuditLogsSchema gained role: z.enum([7 UserRole values]).optional().
#  - BE admin.service.ts getAuditLogs: destructure role + `if (role) where.user = { role };` (Prisma
#    relation filter; User.role is the UserRole enum). Controller/routes unchanged.
#  - FE types/admin.ts: AuditLogQueryParams gained role?: UserRole.
#  - FE AuditLogTab.tsx: new Role <Select> (All Roles + SYSTEM_ROLE_ORDER/SYSTEM_ROLE_CONFIG labels,
#    the latter was already imported-but-unused); passes role into useAuditLogs; selecting a role also
#    narrows the User dropdown to that role (visibleUsers memo, ApiUserListItem.role) and resets
#    userFilter to 'all'. Reverses the earlier "skipped" note for this row.
# BE tsc 0, FE tsc 91 (baseline). No schema change, no migration.
docs/silveroak_backend/src/modules/admin/admin.schema.ts
docs/silveroak_backend/src/modules/admin/admin.service.ts
src/types/admin.ts
src/components/superadmin/AuditLogTab.tsx

# Bugfix: Audit Logs (Super Admin) — "One or more fields have validation errors" on filter + remove
# the specific-user dropdown. The role filter now covers the need; the individual-user dropdown was the
# erroring/redundant control.
#  - BE admin.schema.ts queryAuditLogsSchema: role enum now `.optional().catch(undefined)` so ANY
#    non-role value (the FE 'all' sentinel, a stale/empty value) falls back to no-filter instead of a 400.
#  - FE AuditLogTab.tsx: removed the User <Select> + userFilter state + visibleUsers memo + the entire
#    useUsers query (dropped from the use-admin-api import) + the user_id param + the usersQuery.error
#    branch in errorMessage. The Role filter (SYSTEM_ROLE_ORDER/CONFIG) stays.
#  - Operational: hard-restart the backend (tsx watch can miss a schema reload) + hard-refresh the browser.
# BE tsc 0, FE tsc 91 (baseline). No schema change, no migration.
docs/silveroak_backend/src/modules/admin/admin.schema.ts
src/components/superadmin/AuditLogTab.tsx

# Revert: No Dues approval no longer closes the student's placement & internship process.
# Undoes the 2026-08-04 "approved No Dues CLOSES placement/internship" behavior — a student with an
# approved/issued No Dues can again Apply, Show Interest, and Request a new NOC. SURGICAL revert (the files
# also carry later filter + interest-approval work, so NOT a file restore). The SEPARATE "one approved
# No-Dues record per student" guard is INTENTIONALLY KEPT (assertNoApprovedNoDues + its 4 call sites in
# no-dues.service.ts, and hasApprovedNoDues in no-dues-block.ts, all untouched).
#  - BE no-dues-block.ts: deleted assertPlacementOpenForNoDues (+ NO_DUES_PROCESS_CLOSED). Kept hasApprovedNoDues.
#  - BE application.service.apply() / noc.service.createNoc() / student.service.registerInterests(): removed the
#    assertPlacementOpenForNoDues pre-check + its import. student.service.getMyProfile: dropped no_dues_closed.
#  - FE types/student.ts: removed ApiStudentProfile.no_dues_closed.
#  - FE OpportunityDetail.tsx: removed the no_dues readiness reason + dep-array entry.
#  - FE Dashboard.tsx: removed noDuesBlockReason + amber banner; interestBlockReason reverted to offerBlockReason.
#  - FE NOCDashboard.tsx: removed noDuesClosed + the "process closed" Alert + the Request-New-NOC disable/gating
#    (removed the now-unused useStudentProfile). Kept this session's posting-type filter.
#  - FE noDuesModule.ts: removed the unused hasApprovedNoDues(requests) convenience helper.
# BE tsc 0, FE tsc 91 (baseline). No schema change, no migration.
docs/silveroak_backend/src/shared/utils/no-dues-block.ts
docs/silveroak_backend/src/modules/applications/application.service.ts
docs/silveroak_backend/src/modules/noc/noc.service.ts
docs/silveroak_backend/src/modules/students/student.service.ts
src/types/student.ts
src/pages/OpportunityDetail.tsx
src/pages/Dashboard.tsx
src/pages/NOCDashboard.tsx
src/lib/noDuesModule.ts

# Enhancement: Interest withdrawal — audit (admin name + date/time), student message + lock, admin visibility.
# ⚠ MIGRATION REQUIRED: schema.prisma adds InterestRegistration.reviewed_by_name (String?, additive nullable,
#   no backfill — legacy withdrawn rows → null → message falls back to "TPO Admin"). Run from
#   docs/silveroak_backend: npx prisma migrate dev.
# Extends the 2026-08-04 "TPO approval + soft withdraw" workflow (see the AMENDED note in that block above).
#  - AUDIT / ADMIN NAME: withdraw + approve now snapshot the acting admin's name. Controllers pass req.user.name
#    into approveInterestRegistration/withdrawInterestRegistration; the service writes reviewed_by_name alongside
#    reviewed_by/reviewed_at/status_reason. adminStudentInclude selects reviewed_at/reviewed_by_name/status_reason;
#    mapAdminStudent emits them. Student getInterests already returns the full row → reviewed_by_name flows out.
#  - ADMIN LIST shows WITHDRAWN by default: getInterestRegistrations default status filter changed from
#    {not:'withdrawn'} to undefined (all statuses). getInterestSummary counts still exclude withdrawn.
#    FE InterestLists default filter 'active'→'all'; a Withdrawn/Approved row now shows the review date/time +
#    admin name (+ withdraw reason) under the status badge. Reinstate/Withdraw buttons unchanged.
#  - STUDENT LOCK: registerInterests now THROWS BusinessRuleError INTEREST_WITHDRAWN (422) when a withdrawn row
#    exists (was: reactivate → pending). Only a TPO admin reinstates (approve).
#  - STUDENT MESSAGE: Dashboard no longer hides withdrawn rows — a withdrawn posting type shows a red "Withdrawn"
#    badge + "You have been withdrawn from this Posting Type by TPO Admin on [Date] by [Admin Name]." (+ reason)
#    and the Register button is disabled/locked. ApiInterest + ApiAdminStudentInterest gain reviewed_at/
#    reviewed_by_name/status_reason.
# PRESERVED from the prior fix: visibility = pending∪approved + apply gate = approved (posting-type-interest.ts),
#   the approve/withdraw endpoints + interest_lists:approve permission, and summary/faculty/notification/report
#   exclusions of withdrawn. Events/drives gating for a withdrawn posting type is OUT OF SCOPE (separate follow-up).
# BE tsc 0, FE tsc 91 (baseline). User runs: npx prisma migrate dev.
docs/silveroak_backend/prisma/schema.prisma
docs/silveroak_backend/src/modules/admin/admin.controller.ts
docs/silveroak_backend/src/modules/admin/admin.service.ts
docs/silveroak_backend/src/modules/students/student.service.ts
src/types/student.ts
src/types/admin.ts
src/pages/admin/InterestLists.tsx
src/pages/Dashboard.tsx

# Bugfix: assigned Faculty Coordinator couldn't see the event under Faculty → Department Events.
# Root cause: the event's coordinator was a FREE-TYPED name string (EventEditorDialog <Input>), but the
# backend scoped a faculty's events by an EXACT name match — applyVisibilityScope (event.service.ts)
# used `where.faculty_coordinators = { has: user.name }`, and markAttendance guard used
# `.includes(user.name)` — so titles/spacing/typos ("Prof. Ramesh Kumar" vs account "Ramesh Kumar")
# never matched → faculty saw nothing. (GET /events already faculty-scopes, so the fetch was fine.)
# Fix (user-confirmed): link the coordinator to a real faculty USER identity and scope by user id.
# ⚠ MIGRATION REQUIRED: schema.prisma adds Event.faculty_coordinator_ids String[] @default([]) (additive,
#   legacy-mirror alongside faculty_coordinators names). Run: cd docs/silveroak_backend && npx prisma migrate dev
#   (prisma generate already run; the DB column is added by the migrate.)
# Backend:
#  - event.schema.ts: create schema gains faculty_coordinator_ids: z.array(z.string()).default([]) (updateEvent
#    = createEventSchema.partial() covers it).
#  - event.service.ts: createEvent persists faculty_coordinator_ids (updateEvent spreads {...data});
#    applyVisibilityScope faculty branch → `where.faculty_coordinator_ids = { has: user.id }`; markAttendance
#    guard selects/checks faculty_coordinator_ids.includes(user.id). List/detail use include: (all scalars), so
#    the field flows to the FE automatically.
# Frontend:
#  - types/event.ts: CreateEventInput + ApiEventListItem + ApiEventDetail gain faculty_coordinator_ids: string[].
#  - eventModule.ts: EventFormValues.faculty_coordinators(string) → faculty_coordinator_ids(string[]);
#    buildCreateEventPayload(values, coordinatorNames=[]) sends ids + names; eventDetailToFormValues prefills ids.
#  - EventEditorDialog.tsx: the free-text Faculty Coordinators <Input> replaced by a SearchableMultiSelect of
#    real faculty accounts (useUsers({role:'faculty_coordinator',is_active:'true'}) → value=id,label=name);
#    handleSubmit resolves the selected ids → names and passes both to the payload.
#  - schemas.ts: eventFormSchema gains faculty_coordinator_ids.
# ⚠ Legacy events (free-typed names, no ids) still won't appear for faculty until an admin re-opens the event and
#   re-selects the coordinator(s) — no backfill (names can't be reliably mapped to user accounts).
# BE tsc 0, FE tsc 91 (baseline). User runs: npx prisma migrate dev.
docs/silveroak_backend/prisma/schema.prisma
docs/silveroak_backend/src/modules/events/event.schema.ts
docs/silveroak_backend/src/modules/events/event.service.ts
src/types/event.ts
src/lib/eventModule.ts
src/lib/schemas.ts
src/components/drives/EventEditorDialog.tsx

# Bugfix: Faculty → Department Students export failed with an "export limit" error
# Root cause: the export did a SINGLE fetch with limit:5000, but the backend caps the pagination limit
#   at 100 (paginationSchema.max(100)), so Zod rejected the request → "Unable to export student list."
# Fix (FE-only, no backend change, no migration): handleExport now PAGES THROUGH the list endpoint at
#   limit:100 (the backend max) and accumulates every matching student. Page count is derived from the
#   reliable pagination.total (facultyService pagination is snake/camel-broken — totalPages is undefined
#   at runtime, so it must NOT be read); a runaway guard breaks if a page returns 0 rows.
#   EXPORT_LIMIT(5000) → EXPORT_PAGE_SIZE(100). All matching rows now export, filter-scoped.
# Note: the Date Range filter was ALREADY present (dateRange → sharedFilters → both list + export) and
#   already applies to the export — no code change needed for it, only verification.
# FE tsc 91 (baseline, zero net-new).
src/pages/faculty/DepartmentStudents.tsx

# Enhancement: remove Print & PDF export buttons app-wide (keep CSV/Excel "Export" only)
# There is NO client-side PDF export in the app (no jspdf/html2canvas/react-to-print) — nothing to remove
#   there. The only "PDF"-labeled buttons (NOC "Download PDF", No-Dues "Download NDC") download the actual
#   generated certificate file and are DELIBERATELY KEPT (not table/report exports).
# The shared ReportToolbar/ReportExportHeader were already Print/PDF-free (CSV/Excel only) — ~33 reports
#   using them needed no change.
# Print survived only as 4 inline window.print() buttons, all removed (+ their now-unused Printer/Eye icon
#   imports); the sibling CSV/Export (and Edit/Delete on the policy sheet) buttons stay:
#  - reports/PortfolioCompletionReport.tsx (Print next to Export)
#  - reports/CompanyInternshipSummary.tsx  (Print next to CSV)
#  - reports/CertificatePendingReport.tsx  (Print next to CSV)
#  - admin/PolicyRepository.tsx            ("Print Preview" in the policy detail sheet)
# Verified: grep window.print() → 0 results. FE-only, no migration. FE tsc 91 (baseline, zero net-new).
src/components/reports/PortfolioCompletionReport.tsx
src/components/reports/CompanyInternshipSummary.tsx
src/components/reports/CertificatePendingReport.tsx
src/pages/admin/PolicyRepository.tsx

# Enhancement: Column sorting on management tables — PHASE 1 (server-side, admin + superadmin + faculty)
# Adds clickable column-header sorting to ~20 high-traffic management list tables. Server-side: a header click
#   sends sort_by/sort_order to the API so the WHOLE dataset is sorted across pages (not just the visible page),
#   and pagination resets to page 1.
# Shared FE primitives (NEW):
#  - src/components/shared/SortableTableHead.tsx — clickable <TableHead> (neutral ArrowUpDown → ArrowUp/ArrowDown),
#    className passthrough for text-right/hidden/width, aria-sort.
#  - src/hooks/use-server-sort.ts — useServerSort<TKey>(default, dir, onChange) two-state toggle; onChange resets page.
# Backend: per-module getXOrderBy(sort_by, sort_order) WHITELISTS (mirror faculty getStudentOrderBy) replace the raw
#   `orderBy: { [sort_by || default]: sort_order }` (which injected sort_by into Prisma unvalidated) — closes that
#   Prisma-injection/runtime-error surface. Relational columns → nested orderBy ({ student:{full_name} }, { company:{name} }),
#   Drives Panels/Students → { panels/assigned_students: { _count } }, posting type → { posting_type_master:{value} }.
#   Added Prisma namespace import where missing. NO migration (only orderBy logic).
# FE sort_by union types tightened from string → exact whitelist unions (mirror BE) so tsc rejects a non-whitelisted
#   column; useServerSort<TKey> parameterized to the same union per page.
# Deferred to Phase 2 (endpoints not server-sortable / not server-paginated): PortfolioMonitoringTab, InterestLists,
#   FacultyPrograms, EligibleStudentsTab, SelectionDatabaseTab, CircularsManagement/FacultyCirculars, and the 32 reports.
#   Excluded: RolesPermissionsTab (role×module matrix). Non-sortable columns (Actions, Compensation, Compliance, Mock
#   Result, Posting Types, Audience, Application Window, CGPA, etc.) render plain <TableHead>.
# Verified: BE tsc 0, FE tsc 91 (baseline, zero net-new).
# --- Backend (services: getXOrderBy whitelists) ---
docs/silveroak_backend/src/modules/offers/offer.service.ts
docs/silveroak_backend/src/modules/applications/application.service.ts
docs/silveroak_backend/src/modules/employers/employer.service.ts
docs/silveroak_backend/src/modules/admin/admin.service.ts
docs/silveroak_backend/src/modules/noc/noc.service.ts
docs/silveroak_backend/src/modules/events/event.service.ts
docs/silveroak_backend/src/modules/announcements/announcement.service.ts
docs/silveroak_backend/src/modules/no-dues/no-dues.service.ts
docs/silveroak_backend/src/modules/internships/internship.service.ts
docs/silveroak_backend/src/modules/postings/posting.service.ts
# --- Frontend shared primitives (NEW) ---
src/components/shared/SortableTableHead.tsx
src/hooks/use-server-sort.ts
# --- Frontend types (sort_by unions) ---
src/types/offer.ts
src/types/application.ts
src/types/employer.ts
src/types/admin.ts
src/types/noc.ts
src/types/event.ts
src/types/announcement.ts
src/types/noDues.ts
src/types/internship.ts
src/types/posting.ts
src/types/faculty.ts
# --- Frontend admin page tables ---
src/pages/admin/OffersManagement.tsx
src/pages/admin/ApplicationsManagement.tsx
src/pages/admin/ApplicationPipeline.tsx
src/pages/admin/AdminNOCManagement.tsx
src/pages/admin/DrivesManagement.tsx
src/pages/admin/AnnouncementManagement.tsx
src/pages/admin/NoDuesManagement.tsx
src/pages/admin/InternshipsManagement.tsx
src/pages/admin/PostingsManagement.tsx
# --- Frontend admin/superadmin component tables ---
src/components/admin/CompanyListTab.tsx
src/components/admin/RecruiterListTab.tsx
src/components/admin/StudentListTab.tsx
src/components/superadmin/UserManagementTab.tsx
src/components/superadmin/AuditLogTab.tsx
# --- Frontend faculty tables ---
src/pages/faculty/DepartmentStudents.tsx
src/pages/faculty/EmployerDirectory.tsx
src/pages/faculty/FacultyInternships.tsx
src/pages/faculty/FacultyNOCApprovals.tsx
src/pages/faculty/FacultyOffers.tsx
src/pages/faculty/FacultyAnnouncements.tsx
# --- Frontend consumers touched for tightened unions (as const) ---
src/components/admin/VerificationTab.tsx
src/components/drives/EventAssignmentDialog.tsx

# Enhancement: Column sorting — PHASE 2 (client-side; deferred admin/faculty/recruiter/student tables)
# Extends Phase 1 to the remaining list tables that are NOT server-paginated (they load their full set in
#   memory). REPORTS ARE EXCLUDED per request. RolesPermissionsTab (role×module matrix) stays excluded.
# NEW shared primitive: src/hooks/use-client-sort.ts — useClientSort<T>(rows, accessors, default?, dir?) →
#   { sort_by, sort_order, onSort, sorted }. Accessor per sortable column returns the compare value; two-state
#   toggle; empties sort last; numeric-aware locale compare. Pairs with the existing SortableTableHead.
# Client-sorted tables (render `sorted` instead of the raw array; skip Actions/computed columns):
#   PortfolioMonitoringTab, EligibleStudentsTab (Match%/Skills/Contact left plain), SelectionDatabaseTab,
#   InterestLists (useClientSort placed AFTER its interestFor/registeredAtFor/semesterFor helpers — accessors
#   call them), CircularsManagement, FacultyPrograms (Status→source), FacultyCirculars, StudentCirculars.
# RecruitmentPipeline: the applicants table is rendered INSIDE a per-posting .map (can't call a hook there),
#   so it was extracted into a `PostingApplicantsTable` child component that owns useClientSort.
# RecruiterInternships is genuinely server-paginated → uses SERVER sort (useServerSort + useInternships), like
#   Phase 1, NOT client sort.
# No backend change, no migration. FE tsc 91 (baseline, zero net-new).
# --- Frontend shared primitive (NEW) ---
src/hooks/use-client-sort.ts
# --- Frontend client-sorted tables ---
src/components/admin/PortfolioMonitoringTab.tsx
src/components/admin/EligibleStudentsTab.tsx
src/components/admin/SelectionDatabaseTab.tsx
src/pages/admin/InterestLists.tsx
src/pages/admin/CircularsManagement.tsx
src/pages/faculty/FacultyPrograms.tsx
src/pages/faculty/FacultyCirculars.tsx
src/pages/StudentCirculars.tsx
src/pages/recruiter/RecruitmentPipeline.tsx
# --- Frontend server-sorted table (Phase-1 pattern) ---
src/pages/recruiter/RecruiterInternships.tsx

# Bugfix: NOC wizard State→City filter (City now cascades from the selected State)
# Reported: Student → Apply for NOC → Step 2, selecting a State did not filter/populate City (both
#   University-drive and Self-sourced). Root cause: the cascade was NEVER built — State is a SearchableSelect
#   over INDIAN_STATES while City was a free-text <Input>+<datalist> fed by the FLAT, global cities[] from
#   /noc/field-suggestions (never scoped by state); no state→city dataset existed anywhere.
# Fix (FE-only, NO migration): new curated India state→cities map + a state-scoped City dropdown that mirrors
#   the existing State field pattern.
#  - NEW src/lib/indianCities.ts: INDIAN_STATE_CITIES (keys match INDIAN_STATES exactly) + getCitiesForState().
#    Curated major cities per state/UT — intentionally NOT exhaustive (an "Other" free-text escape covers the rest).
#  - NOCRequestWizard.tsx: added cityOther state; cityOptions memo (getCitiesForState(company_state), merges the
#    current company_city so an edited value stays selectable) + cityPinnedOptions ("Other – Enter New City");
#    City renders as a SearchableSelect scoped to the state (+ Other → free-text Input with the existing backend
#    datalist hints) when the state is mapped and not the free-typed "Other" state, else falls back to the old
#    free-text Input ("Select a state first" placeholder). Changing State clears company_city + resets cityOther
#    (updateField is functional, so the paired city/state clears don't clobber). cityOther also reset in the full
#    form reset and the placement-source change handler. Validation unchanged (company_city/state stay strings).
# Verified: FE tsc 91 (baseline, zero net-new).
src/lib/indianCities.ts
src/components/noc/NOCRequestWizard.tsx

# Enhancement: NOC Internship Completion Certificate (submit → TPO approve → portfolio + Issued consolidation)
# ⚠ MIGRATION REQUIRED — schema.prisma: new enum CompletionCertStatus { pending, approved, rejected } +
#   NocRequest columns completion_certificate_url/_name/_mime_type/_size, completion_status,
#   completion_submitted_at, completion_reviewed_by/_by_name/_at, completion_remarks, completion_due_notified_at.
#   All additive/nullable (auto-migratable, no backfill). Run: cd docs/silveroak_backend && npx prisma migrate dev
#   (npx prisma generate already run). Restart BE.
# Completion tracking is a SEPARATE lifecycle from NOC issuance — the NOC stays `issued`; completion_status
#   tracks the uploaded certificate (null=not submitted → pending → approved/rejected).
# Student: on the Completed tab, issued NOCs show an "Upload Completion Certificate" button (re-upload when
#   rejected) + a status badge + a "Completion Certificate" view link. Lazy end-date reminder (no cron exists):
#   getMyNocs fires a one-time notification per issued NOC whose end_date has passed with no cert submitted
#   (completion_due_notified_at idempotency).
# TPO: new "Completion Certificates" tab (pending list) → CompletionReviewDialog Approve/Reject (mandatory
#   remark on reject; student may re-upload → Pending). Approval creates a student Certification (portfolio) +
#   recalcProfileCompletion (best-effort, idempotent by document_url) and notifies the student. Every
#   submit/approve/reject is written to the audit log (history). Issued tab consolidates per student: Offer
#   Letter + NOC Certificate + Completion Certificate links + completion status/approval-date/remarks.
# Endpoints (noc.routes): POST /noc/:nocId/completion-certificate (student, multer, upload+attach),
#   PUT /noc/:nocId/completion-certificate/approve|reject (tpo, noc_requests:approve). getNocs gains a
#   completion_status filter. Verified: BE tsc 0, FE tsc 91 (baseline).
# --- Backend ---
docs/silveroak_backend/prisma/schema.prisma
docs/silveroak_backend/src/middleware/upload.ts
docs/silveroak_backend/src/modules/noc/noc.service.ts
docs/silveroak_backend/src/modules/noc/noc.schema.ts
docs/silveroak_backend/src/modules/noc/noc.controller.ts
docs/silveroak_backend/src/modules/noc/noc.routes.ts
docs/silveroak_backend/src/modules/students/student.service.ts
# --- Frontend ---
src/types/noc.ts
src/services/nocService.ts
src/hooks/use-noc-api.ts
src/lib/nocModule.ts
src/components/noc/CompletionCertificateDialog.tsx
src/components/noc/CompletionReviewDialog.tsx
src/components/noc/NOCRequestCard.tsx
src/components/noc/NOCRequestDetailSheet.tsx
src/pages/admin/AdminNOCManagement.tsx

# Enhancement: TPO Admin → Applications → Application Pipeline — "View Resume" button per applicant
# The per-posting applicant list (View/Profile/Move/Mock actions) had no way to open the resume the
#   student submitted for THAT posting. Added a "View Resume" button to each row that opens exactly the
#   application's resume in a new tab (resolveBackendAssetUrl → API base, works behind the prod proxy);
#   disabled with a "No resume submitted" tooltip when the applicant has no resume_url. FE-ONLY, no
#   backend/schema/migration — ApiApplicationListItem already carries resume_url and useApplications
#   already returns it. Verified: FE tsc 91 (baseline, zero net-new).
src/pages/admin/ApplicationPipeline.tsx

# Enhancement: TPO Admin → Offer & Joining Management → Create Offer — Posting Type filter
# Added a "Posting Type" dropdown at the top of the Create Offer dialog. Selecting a type filters the
#   Posting dropdown to only postings of that type (passed to usePostings as posting_type_master_id,
#   server-side; "All posting types" restores the full list). Changing the type clears any selected
#   posting; the type filter resets to "all" when the dialog closes. UI-only filter — NOT part of the
#   offer payload. FE-ONLY, no backend/schema/migration. Verified: FE tsc 91 (baseline, zero net-new).
src/components/offers/CreateOfferDialog.tsx

# Enhancement: TPO Admin → Events & Drives — Application Pipeline Stage eligibility + Eligible Student Count
# ⚠ MIGRATION REQUIRED: schema.prisma → model Event gains `application_stage ApplicationStage?` (nullable,
#   grandfathers existing events; additive/auto-migratable). Run: cd docs/silveroak_backend && npx prisma migrate dev
# Create/Edit Event now has an "Application Pipeline Stage" dropdown (All/Applied/Mock Round/Shortlisted/
#   Test Scheduled/Interview/HR Round/Offer Released/Rejected) below Linked Postings. When a stage is set,
#   only students whose CURRENT application stage on a linked posting matches are auto-assigned to the event
#   (snapshot at create/update, add-only — never removes marked attendance/panels/manual assignments). "All"
#   = every applicant (prior behaviour). resolveApplicantStudentIds gained a stage filter. Attendance list =
#   the assigned snapshot (already the stage-filtered eligible set — no separate filtering needed).
# Events & Drives listing gains an "Eligible Students" column = LIVE distinct count of students matching the
#   event's linked postings + configured stage (countEligibleStudents, batched in getEvents; also in CSV/Excel
#   export). Verified: BE tsc 0, FE tsc 91 (baseline, zero net-new).
# --- Backend ---
docs/silveroak_backend/prisma/schema.prisma
docs/silveroak_backend/src/modules/events/event.schema.ts
docs/silveroak_backend/src/modules/events/event.service.ts
# --- Frontend ---
src/types/event.ts
src/lib/eventModule.ts
src/components/drives/EventEditorDialog.tsx
src/pages/admin/DrivesManagement.tsx

# Enhancement: TPO Admin Dashboard — show the 10 most recent Interest Registration records
# The dashboard "Interest Registrations" card showed aggregate counts by posting type; it now lists the
#   10 most recent individual registration RECORDS (newest first: student name, posting-type label, date,
#   status badge), replacing the aggregate summary. New BE endpoint GET /admin/interests/registrations/recent
#   (getRecentInterestRegistrations → interestRegistration.findMany orderBy registered_at desc, take 10,
#   all statuses, interest_lists:view). FE new type/service/hook + card rewrite; removed the now-unused
#   summary hook/derivation on the dashboard (getInterestSummary/InterestLists unchanged). No migration.
#   Verified: BE tsc 0, FE tsc 91 (baseline, zero net-new).
# --- Backend ---
docs/silveroak_backend/src/modules/admin/admin.service.ts
docs/silveroak_backend/src/modules/admin/admin.controller.ts
docs/silveroak_backend/src/modules/admin/admin.routes.ts
# --- Frontend ---
src/types/admin.ts
src/services/adminService.ts
src/hooks/use-admin-api.ts
src/pages/admin/AdminDashboard.tsx

# Enhancement: Student No Dues → Request No Dues Certificate — dynamic form per Exit Reason
# ⚠ MIGRATION REQUIRED: schema.prisma → model NoDuesRequest gains 7 nullable columns
#   (sou_passing_year, company_sector, company_address, language_test, university_address,
#   examination_name, additional_details). Run: cd docs/silveroak_backend && npx prisma migrate dev
# Exit Reason is now a dropdown (5 options) driving a fully dynamic field set + per-reason proof helper text:
#   Job/Employment (passing year, company, designation, sector, package CTC, company address, joining date),
#   Business (passing year, business name/sector/address), Planning-for-Further-Studies (passing year,
#   country, intended course=program_name, preferred university=institution_name, language test — required
#   only when country != India), Admission-Taken (passing year, country, course/program=program_name,
#   university name=institution_name, university address), Competitive-Exam (passing year, examination name,
#   additional details=optional). SOU Passing Year = generated year range (prefilled from student.batch);
#   Country = new src/lib/countries.ts dropdown (India-vs-Abroad flips language-test requirement + proof
#   helper text for Planning/Admission). Reused columns: company_name/designation/package_lpa/joining_date/
#   business_*/program_name/institution_name/country; single proof_url per request (upload-first, PDF-only).
#   Backend stays lenient (all fields optional); the FE Zod discriminated union enforces per-reason required.
#   Admin No Dues detail sheet extended to show all new fields incl. the previously-missing planning/exam
#   branches. Verified: BE tsc 0, FE tsc 90 (<=91 baseline; the 2 NoDuesManagement errors are pre-existing).
# --- Backend ---
docs/silveroak_backend/prisma/schema.prisma
docs/silveroak_backend/src/modules/no-dues/no-dues.schema.ts
docs/silveroak_backend/src/modules/no-dues/no-dues.service.ts
# --- Frontend ---
src/types/noDues.ts
src/lib/schemas.ts
src/lib/countries.ts
src/lib/noDuesModule.ts
src/pages/NoDuesCertificate.tsx
src/pages/admin/NoDuesManagement.tsx

# Enhancement: TPO Admin → Application Pipeline → Application Records — Event attendance column
# Added an "Attendance for Event" dropdown (SearchableSelect of recent events via useEvents) above the
#   records and a new "Attendance" column. On selecting an event, each applicant row shows Present / Absent
#   / Late (from the event's assigned_students attendance) or "—" when the student has no attendance record
#   for that event (or no event selected). Data via useEventDetail(eventId).assigned_students keyed by
#   student_id; matched to application.student.id. FE-ONLY, no backend/schema/migration. Verified: FE tsc 90.
src/pages/admin/ApplicationPipeline.tsx

# Enhancement: Hide Master→Branches & Placement Cell Programs; No Dues heading shows student name (not UUID)
# 1) TPO → Master Data: hid the "Branches" section — removed 'branch' from ADMIN_SECTION_ORDER and changed
#    the default activeSection from 'branch' to 'skill' (MasterDataManagement.tsx). Config/type/order kept
#    (reversible). 2) TPO sidebar: commented out the "Placement Cell Programs" nav item (AppSidebar.tsx);
#    the /admin/internships route is left intact (hidden from nav only). 3) No Dues Certificate heading:
#    replaced the bare record UUID with the student's name — admin sheet uses selectedRequest.student_name;
#    student RequestCard gets a new studentName prop (= profile student.full_name, since getMyNoDues omits
#    the student relation so request.student_name is empty in that view). FE-ONLY, no backend/schema/migration.
#    Verified: FE tsc 90 (baseline; only pre-existing NoDuesManagement errors remain).
src/pages/admin/MasterDataManagement.tsx
src/components/layout/AppSidebar.tsx
src/pages/admin/NoDuesManagement.tsx
src/pages/NoDuesCertificate.tsx

# Enhancement: Faculty portal — hide Notifications tab & Internships nav; add read-only offer View slide-over
# 1) Faculty → My Profile: hid the "Notifications" tab. MyProfile (shared) gained an optional
#    showNotifications prop (default true); FacultyProfile passes showNotifications={false}. Other roles
#    unaffected. 2) Faculty → Offer & Joining Status: added an Actions "View" (Eye) button per row that opens
#    the existing OfferDetailSheet in a new readOnly mode. OfferDetailSheet gained readOnly?: boolean (+
#    onReject/onUpdateJoining now optional) — readOnly hides the Reject/Update-Joining buttons and the
#    editable Compliance Select/Switch/Save panel, keeping compliance as read-only badges + all info sections
#    + audit trail. 3) Faculty sidebar: commented out the "Internships" nav item (route left intact).
#    FE-ONLY, no backend/schema/migration. Verified: FE tsc 90 (baseline).
src/pages/shared/MyProfile.tsx
src/pages/faculty/FacultyProfile.tsx
src/components/offers/OfferDetailSheet.tsx
src/pages/faculty/FacultyOffers.tsx
src/components/layout/AppSidebar.tsx
